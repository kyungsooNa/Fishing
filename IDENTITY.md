# 누구의 감시인가 (기기 간 동기화 결정 기록)

지금 감시 목록은 **브라우저에 묶여 있습니다.** 무작위 토큰 하나를 `localStorage`에 두고
서버가 그 해시로 목록을 나눕니다(`core/watchers.js`, #140). 계정이 없어 가볍지만 두 가지가
안 됩니다.

1. **폰에서 건 감시를 PC에서 못 봅니다.** 토큰이 그 브라우저에만 있습니다.
2. **알림이 사람마다 안 갑니다.** 채널(텔레그램·디스코드)이 서버 전역이라, 누가 건 감시든
   같은 채팅방으로 갑니다(`core/notify.js`).

둘은 다른 문제처럼 보이지만 뿌리가 같습니다 — **서버가 "이 사람"을 부를 이름이 없습니다.**

## 후보

| | 기기 간 | 사람별 알림 | 운영 부담 | 사용자가 하는 일 |
|---|---|---|---|---|
| A. 계정(이메일·비밀번호) | ○ | ○ | **큼** — 비밀번호 재설정·개인정보·탈퇴 | 가입 |
| B. 토큰 공유 코드 | ○ | ✗ | 작음 | 다른 기기에 코드 입력 |
| C. 내보내기·가져오기 파일 | △ 수동 | ✗ | 없음 | 파일을 옮김 |
| **D. 알림 채널을 신원으로** | ○ | ○ | 작음 | 봇에게 `/start` 한 번 |
| E. 그대로 두기 | ✗ | ✗ | 없음 | — |

## 고른 것: D — 알림 채널이 곧 신원

**취소석 감시를 쓰려면 어차피 알림 채널을 연결해야 합니다.** 그 연결이 이미 "이 사람"을
가리키므로, 신원을 따로 만들 이유가 없습니다.

- 텔레그램 봇에게 `/start`를 보내면 서버가 그 대화의 `chat_id`를 받습니다. 그게 이름입니다.
- 화면에서는 봇이 알려준 **짧은 연결 코드**를 한 번 입력합니다. 그 순간 이 브라우저의
  감시 목록이 그 채널로 넘어갑니다 — **`adopt`가 이미 하는 일**입니다(`core/monitor.js`).
- 다른 기기에서도 같은 코드를 넣으면 같은 목록을 봅니다. 기기 간 문제는 이렇게 풀립니다.
- 알림은 그 사람의 채널로만 갑니다. 두 번째 문제도 같이 풀립니다.

**계정은 만들지 않습니다.** 저장하는 것은 채널 식별자의 해시뿐이고, 이메일도 비밀번호도
받지 않습니다. 잃어버리면 봇에게 다시 `/start` 하면 됩니다 — 재설정 절차가 필요 없습니다.

**채널을 연결하기 전에도 감시는 됩니다.** 지금처럼 브라우저 토큰으로 걸어두고, 나중에
연결할 때 그 목록을 넘깁니다. 알림을 안 받겠다는 사람에게 연결을 강요하지 않습니다.

## 고르지 않은 이유

- **A. 계정** — 이 규모에 비해 운영 부담이 큽니다. 비밀번호를 다루는 순간 재설정·유출·탈퇴가
  따라오고, 그 대가로 얻는 것은 D가 이미 주는 것뿐입니다.
- **B. 토큰 공유 코드** — 기기 간은 되지만 알림은 여전히 전역입니다. D가 같은 값에 하나 더 줍니다.
- **C. 내보내기·가져오기** — 서버가 필요 없어 매력적이지만 사람이 매번 파일을 옮겨야 하고,
  감시는 서버에 있어야 도는 기능이라 반쪽입니다. 다만 **즐겨찾기·별점에는 이게 맞습니다**(아래).
- **E. 그대로** — 폰으로 보고 PC에서 거는 게 지금 못 하는 일인데, 낚시는 폰으로 봅니다.

## 즐겨찾기·별점은 신원을 따로 두지 않습니다

즐겨찾기와 별점은 **서버로 보내지 않는 개인 기록**입니다(`AGENTS.md`). 읽기 전용 화면
(GitHub Pages)에서도 되는 것이 그 덕입니다. 여기에 신원을 붙이면 그 성질이 깨집니다.

기기 간에 옮기고 싶으면 **C(내보내기·가져오기)**로 충분합니다 — `localStorage`의
`fishing:favorites`·`fishing:ratings`를 파일로 내보내고 다른 기기에서 읽어들이는 것.
감시(서버)와 개인 기록(브라우저)의 경계를 그대로 둡니다.

## 다음 단계 (이 결정이 남긴 것)

1. 텔레그램 봇에 `/start` 처리와 연결 코드 발급 — 봇이 코드를 알려주고 서버가 코드↔`chat_id`를 기억합니다.
2. 화면에 "알림 채널 연결" 칸 — 코드를 넣으면 이 브라우저의 감시가 그 채널로 넘어갑니다.
3. `notify()`가 채널을 인자로 받게 — 지금은 환경변수 하나를 봅니다.
4. 즐겨찾기·별점 내보내기·가져오기 (선택).

1~3은 상시 서버가 떠야 값이 있습니다(`DEPLOY.md`). 서버 없이 만들면 확인할 방법이 없습니다.
