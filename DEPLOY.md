# 상시 감시를 어디서 돌릴 것인가 (결정 기록)

`PRODUCT.md`가 말하는 이 제품의 핵심은 **취소석 레이더**입니다. 그런데 지금은 사용자의 PC와
브라우저가 켜져 있는 동안에만 감시가 돕니다. 그 상태로는 "자리 나면 알려준다"가 성립하지
않습니다. 어디서 돌릴지를 여기서 정합니다.

이 파일은 **결정과 그 근거**만 적습니다. 어떻게 배포할지(설치 절차)는 그 구조를 실제로
띄울 때 `README.md`에 적습니다.

## 1. 지금 상태 — 잰 값

수집 한 바퀴 (2026-09-08, 활성 267곳 기준):

| 값 | 측정 |
|---|---|
| 한 바퀴 요청 수 | **498회** (선상24 154곳 × 1 + 더피싱 상세 111곳 × 3 + 그 외 11) |
| 가장 큰 직렬 그룹 | `thefishing.kr` **288요청** — 호스트당 3초라 이 그룹만 **14.4분** |
| 한 바퀴 실제 소요 | 8~16분 (Actions 실행 기록) |
| 브라우저 필요 | 없음 (`npm run needs-browser` → false, 전부 static) |
| `docs/data.json` | 6.9MB |

GitHub Actions 크론(`10 * * * *`, 매시 10분)이 실제로 언제 돌았는지 — 최근 77시간:

| 값 | 측정 |
|---|---|
| 예정 대비 실행 | 77회 예정 → **20회 실행 (26%)** |
| 예정 시각 대비 지연 | 중앙값 **39분**, p90 57분, 최대 59분 |
| 연속 실행 간격 | 중앙값 **3.3시간**, 최대 **8.6시간** |

**취소석은 몇 분 단위 경쟁입니다.** 39분 늦게, 그것도 네 번에 한 번만 도는 감시는 이 제품의
핵심 기능을 못 합니다. 무료 크론이 밀리는 건 GitHub의 알려진 동작이고, 우리가 고칠 수 있는
것이 아닙니다.

여기에 하나 더 있습니다. **Actions 러너는 해외 IP입니다.** 지금 `node metrics.js`를 돌리면
더피싱 111곳이 통째로 timeout 백오프 보류이고, 화면 출조의 37.8%가 12시간 전 값입니다.
같은 주소를 국내에서 열면 정상으로 보입니다.

로컬 서버(`core/monitor.js`)는 관심 출조 3분 · 전체 60분을 실제로 지킵니다. 그러나 PC가
꺼지면 감시도 알림도 같이 멈추고, 사용자는 그 사실을 모릅니다.

## 2. 무엇을 만족해야 하나

1. **국내에서 정상 접근** — 차단을 우회하자는 게 아니라, 국내 사이트를 국내에서 보자는 것입니다.
2. **주기를 실제로 지킬 것** — 관심 출조 3분, 전체 60분. 러너가 언제 뜰지에 기대지 않습니다.
3. **재시작해도 남을 것** — 감시 목록(`tmp/monitor.json`)과 알림 이력(`core/alerts.js`의 JSONL).
   지금 기본 경로가 `tmp/`라 러너에서는 매번 사라집니다.
4. **관리 API는 루프백 원칙 유지** — `/api/*`는 registry를 고치고 프로세스를 띄웁니다
   (`serve.js`의 `adminAllowed`). 원격에 두면 인증이 필요해지고, 그건 이 결정의 다음 단계입니다.
5. **화면은 그대로 Pages** — 서버가 죽어도 현황판은 읽기 전용으로 계속 뜹니다.
6. **운영 부담이 작을 것** — 혼자 굴리는 프로젝트입니다. 손이 많이 가는 구조는 결국 안 돌아갑니다.

## 3. 후보

| | 국내 IP | 3분 주기 | 상태 저장 | 인증 | 비용 | 운영 부담 |
|---|---|---|---|---|---|---|
| **A. GitHub Actions만** (현행) | ✗ 해외 | ✗ 26%·39분 지연 | 커밋으로만 | 불필요 | 0원 (공개 레포) | 없음 |
| **B. 집 PC 상시** (현행 로컬) | ○ | ○ | 파일 | 루프백 | 전기값 | PC를 계속 켜둬야 |
| **C. 국내 소형 VPS 1대** | ○ | ○ | 파일 | SSH 터널 | 월 1만원 안팎 | 서버 1대 |
| D. 해외 VPS + 국내 프록시 | △ 프록시 경유 | ○ | 파일 | 두 대 몫 | 월 2만원 안팎 | 서버 2대 |
| E. 서버리스 크론 | ✗ | ✗ 실행시간 제한 | 외부 저장소 필요 | 토큰 | 0~소액 | 적음 |

비용은 어림값입니다 — 실제 요금제는 살 때 확인합니다. 이 결정은 비용으로 갈리지 않습니다.

## 4. 선택: C. 국내 소형 VPS 한 대

- **요구사항을 다 만족하는 유일한 후보**입니다. 국내 IP, 주기 보장, 파일 저장, 그리고 서버 한 대.
- **사양은 작아도 됩니다.** 브라우저가 필요 없고(전부 static), 요청이 초당 1건도 안 되며
  (498요청/시간 ≈ 0.14건/초), 파싱은 cheerio라 가볍습니다. 1vCPU / 1GB면 충분합니다.
  디스크는 `data.json` 6.9MB에 알림 이력이 조금씩 붙는 정도입니다.
- **코드를 새로 짜지 않습니다.** `core/monitor.js`가 이미 전체 60분 · 관심 출조 3분으로 돌고,
  플랫폼별 직렬·호스트 3초 간격·실패 백오프·중복 실행 방지를 지킵니다. VPS에서는 같은
  프로세스를 그냥 계속 켜둡니다.
- **되돌릴 수 있습니다.** 저장 형식(`docs/data.json`, JSONL 이력)이 그대로라, 서버를 접으면
  Actions만 남기고 예전 상태로 돌아갑니다.

같이 남기는 것:

- **GitHub Actions는 유지합니다.** 다만 역할이 바뀝니다 — 상시 감시가 아니라 **백업 수집과
  Pages 퍼블리시**입니다. 서버가 죽어도 현황판이 몇 시간 안에 한 번은 갱신됩니다.
  `discover`·`peek`도 그대로 Actions에서 돕니다(국내 도메인이 개발 환경에서 막혀 있어서).
- **로컬 서버도 그대로 씁니다.** 어댑터를 고치고 눈으로 확인하는 자리로는 이게 제일 빠릅니다.

## 5. 고르지 않은 이유

- **A. Actions만** — 예정의 26%만, 그것도 39분 늦게 도는 크론으로는 취소석을 못 잡습니다.
  해외 IP 문제(더피싱 111곳 보류)도 그대로입니다. 백업 수집으로는 계속 씁니다.
- **B. 집 PC** — 가장 싸고 이미 됩니다. 그러나 **알림은 한 번 놓치면 신뢰가 무너집니다.**
  PC가 꺼진 걸 사용자는 모르고, "자리 안 났나 보다"로 읽습니다. 검증용으로는 계속 씁니다.
- **D. 해외 VPS + 국내 프록시** — 얻는 건 IP 하나인데 관리할 서버가 두 대가 됩니다.
  국내 VPS가 그 값보다 싸고 단순합니다.
- **E. 서버리스** — 국내 IP가 아니라 A와 같은 문제가 남고, 한 바퀴 15분이 실행시간 제한에
  걸립니다. 상태도 밖에 따로 둬야 합니다.

## 6. 이 결정이 남긴 다음 단계

`TODO.md` 3번의 나머지 항목이 그대로 이어집니다.

- **감시 목록·알림 이력 저장 위치** — 지금은 `tmp/`(러너에서 사라짐). 서버에서는 고정 경로로
  옮기고(`ALERTS_PATH`), 사용자가 여럿이 되면 그때 저장소를 다시 봅니다. 파일로 시작합니다.
- **인증** — 관리 API는 루프백 유지가 원칙입니다. 서버에서는 SSH 터널로 붙고, 공개 화면은
  읽기 전용 Pages 그대로 둡니다. 관리 화면을 바깥에 열어야 할 때 그때 인증을 설계합니다.
- **운영 상태 확인** — `node metrics.js`(커버리지)와 `node alerts.js`(감지 지연·알림 실패)가
  이미 있습니다. 서버에서 이 둘을 주기적으로 찍어두면 그게 운영 지표가 됩니다.
