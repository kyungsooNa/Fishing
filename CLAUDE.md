# 작업 맥락

## 이게 뭐냐

여러 낚시 예약 사이트를 긁어 `docs/data.json` 하나로 모으고, `docs/index.html`이 그걸 읽어
표로 보여줍니다. GitHub Actions가 주기적으로 돌리고 결과를 커밋합니다. 서버는 없습니다.

## 구조

```
collect.js        전체 수집 진입점 (Actions가 이걸 부릅니다)
debug.js          어댑터 고칠 때 쓰는 도구. 한 사이트만 돌려보고 표로 보여줍니다
serve.js          docs/ 로컬 서빙
core/schema.js    통합 스키마 + 표기 정규화. 어댑터는 원문 텍스트만 넘기면 됩니다
core/fetcher.js   HTTP/Playwright, 호스트당 3초 간격, EUC-KR 디코딩
core/store.js     docs/data.json 읽기·쓰기
core/diff.js      이전 결과와 비교해 "새로 난 자리"만 추출
core/merge.js     같은 출조·같은 배 합치기 (신원은 이름+출항지+전화번호)
core/platform.js  사이트 계열(선상24/더피싱/자체). 어댑터에서 자동
core/when.js      날짜 기준. 러너가 UTC여도 전부 한국시간으로 셉니다
core/ports.js     항구 좌표(지도용). sites/ports.json에서 읽습니다
core/notify.js    텔레그램/디스코드. 토큰 없으면 조용히 건너뜀
core/runner.js    전체 순회, 실패 격리, 지난 날짜 정리
adapters/         사이트 유형별 파서. collect(site) 하나만 내보내면 됩니다
adapters/_rows.js 출조 행 읽는 부분. 사이트에 안 얽혀 있어 어댑터들이 같이 씁니다
adapters/generic.js 자체 사이트용. 주소만 적으면 본문 표기로 읽어봅니다
sites/registry.json  ★ 사이트 목록. 보통은 여기만 고칩니다
test/             가상 HTML로 파서 로직만 검증 (npm test)
```

## 설계에서 지킨 것

- **파싱은 클래스명이 아니라 본문 표기로.** "운항시간 / 남은자리 / 예약마감" 같은 글자를
  기준으로 읽습니다. 템플릿이 개편돼도 표기가 남아있는 한 버팁니다.
- **요청은 최소로.** 목록형 sunsang24는 한 달치를 요청 한 번에 받습니다. 더피싱 index 방식은
  요청 한 번에 배 전부 × 4주치가 옵니다. 2주를 보려고 14번 요청하지 않습니다.
- **한 사이트가 죽어도 나머지는 산다.** 실패한 사이트는 직전 결과를 그대로 남기고
  화면 하단 "수집 상태"에 실패로 표시합니다. 화면이 갑자기 비지 않습니다.
- **알림은 값어치 있는 것만.** 취소석과 자리 늘어남만 알립니다. 새로 올라온 일정은
  아무도 예약 안 한 게 당연해서 알리지 않고, 수집 실패한 사이트는 비교에서 뺍니다.
- **같은 배를 합칠 땐 이름만 믿지 않습니다.** 다른 지역에 같은 이름의 배가 있어서,
  이름·출항지·전화번호가 셋 다 있고 셋 다 같을 때만 합칩니다(`core/merge.js`).
  하나라도 비면 안 합칩니다 — 합쳐서 틀리는 것보다 두 줄이 낫습니다.
  세 값은 전부 registry에서 오므로 합칠지 말지는 사람이 정하는 셈입니다.
- **날짜는 전부 한국시간입니다**(`core/when.js`). Actions 러너가 UTC라 그냥 두면
  06:10 수집 때 "오늘"이 어제가 됩니다. 새 코드에서 `new Date()`로 날짜를 만들지 마세요.
- **한글에는 `\b` 단어경계가 없습니다.** 배 이름 정규식은 `(?![가-힣])`로 끊습니다.
  이거 때문에 한 번 틀렸으니 정규식 고칠 때 조심하세요.

## 작업 방식

**변경 하나 = 커밋 하나 = PR 하나.** 여러 변경을 한 브랜치에 쌓아두지 마세요.
사이트 하나 추가, 버그 하나 수정, 기능 하나 — 각각 브랜치를 따고 푸시한 다음
**곧바로 PR을 엽니다.** 물어보고 열지 말고 그냥 여세요.

```bash
git checkout -b <작업이름>
# ... 변경 ...
npm test
git commit -m "..."
git push -u origin <작업이름>
# 푸시했으면 바로 PR 생성. 리뷰는 PR에서 합니다
```

이미 열려 있는 PR에 후속 커밋을 얹는 건 괜찮습니다 — 같은 변경을 마무리하는
경우에 한해서입니다. 성격이 다른 변경이면 브랜치를 새로 땁니다.

푸시만 하고 PR을 안 열면 `main`에 아무것도 안 들어갑니다. GitHub Pages는
`main`의 `/docs`를 보므로, PR이 머지되기 전까지 화면은 그대로입니다.

## 어댑터 고칠 때

```bash
node debug.js              # 등록된 id 목록
node debug.js akbari       # 한 곳만
node debug.js akbari --dump  # 실패 시 원본 HTML을 tmp/ 에 저장
npm test                   # 가상 HTML로 파서 회귀 확인
```

`debug.js`가 "잔여석이 전부 비었다", "날짜가 하루뿐이다" 같은 흔한 증상을 짚어줍니다.

## 아직 확인 못 한 것

`sites/registry.json`의 실제 사이트들은 **국내 IP에서 한 번도 검증되지 않았습니다.**
개발이 이뤄진 환경에서 해당 도메인이 전부 차단(HTTP 403)돼서 실제 HTML을 못 봤습니다.
`test/fixtures.js`는 실제 마크업이 아니라 본문 표기만 흉내 낸 것이라, 테스트가 통과해도
진짜 사이트에서 되는지는 별개입니다. **각 사이트를 처음 켤 때 `node debug.js <id>`로
한 번씩 대조하세요.** 특히:

- sunsang24 목록형이 한 페이지에 며칠치를 보여주는지. 주소는 `/ship/schedule_fleet` 하나만
  부릅니다 — 여러 선사 사이트에서 확인한 형태입니다. 뒤에 숫자가 붙은 경우(`/1359`, `/0`)도
  있는데 연월이 아니라 배 번호로 보입니다. 다음 달까지 주소로 넘길 방법이 있으면
  registry에 `monthPath`를 적으면 그때부터 달을 넘겨가며 받습니다
- 더피싱 index 방식에서 메인 요약표를 실제로 찾는지 (못 찾으면 detail로 자동 폴백)
- detail 방식은 `seatsTotal`이 정확해야 잔여석이 맞습니다

## 알아둘 것

- GitHub Actions 러너는 해외 IP입니다. 국내 사이트가 차단하면 그 사이트만 국내 서버로 옮기세요.
- 요청 간격은 호스트당 3초(`core/fetcher.js`의 `MIN_GAP_MS`). 사이트가 민감하면 늘리세요.
- 워크플로는 브라우저가 필요할 때만 playwright를 받습니다(`core/platform.js`의 `needsBrowser`).
  `"auto"`도 본문이 비면 브라우저로 넘어가므로 같이 셉니다 — `"js"`가 없다고 안전한 게 아닙니다.
- 취소석은 몇 분 단위 경쟁입니다. 알림을 진지하게 쓸 거면 상시 서버에서 5분 주기로 도는 게 맞습니다.
