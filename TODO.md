# 제품 TODO

이 목록은 [제품 방향](PRODUCT.md)을 실제 작업 순서로 옮긴 것입니다. 위에 있는 미완료 항목부터
처리하되, 각 체크박스는 원칙적으로 **변경 하나 = PR 하나**가 되게 나눕니다. 기능을 구현한
PR에서 테스트와 문서를 함께 고치고 이 목록도 체크합니다.

## 이미 갖춘 기반

- [x] 선상24·더피싱·개별 사이트를 같은 출조 스키마로 정규화한다.
- [x] 한 사이트가 실패해도 직전 결과와 나머지 사이트를 유지한다.
- [x] 이름·출항지·전화번호가 모두 같은 배만 합치고 출처별 값을 보존한다.
- [x] 날짜·선사·지역·항구·어종·운항·빈자리로 출조를 거른다.
- [x] 마지막 확인 시각과 수집 실패를 현황판에 표시한다.
- [x] 마감 해제와 잔여석 증가만 감지하고 새 일정은 알리지 않는다.
- [x] 로컬 서버에서 관심 출조를 우선 수집하고 텔레그램·디스코드로 알린다.
- [x] 원본 예약처 링크, 즐겨찾기, 개인 별점, 항구 지도를 제공한다.

## 1. 현재 상태를 숫자로 알기

기능을 더 붙이기 전에 선상24 의존도와 데이터 품질을 계속 측정할 수 있어야 합니다.

- [x] **플랫폼별 현황 명령 추가**
  - 활성 사이트 수, 출조 수, 수집 성공률, 마지막 확인 시각 분포를 출력한다.
  - 선상24·더피싱·자체 사이트를 따로 집계하고 합계가 실제 데이터와 맞는지 테스트한다.
  - 완료 조건: 한 명령으로 현재 커버리지와 실패 비율을 재현할 수 있다.
- [x] **데이터 품질 누락 현황 추가** — `node quality.js` (`core/quality.js`)
  - 항구·전화번호·출항시각·승선료·정원·원본 링크가 없는 출조와 선사를 센다.
  - 합치기 신원에 필요한 항구·전화번호 누락은 일반 표시와 구분한다.
  - 완료 조건: 다음에 손볼 registry와 어댑터를 숫자와 id로 고를 수 있다.
- [x] **취소석 감시 지표 정의 및 기록** — `core/alerts.js`, `node alerts.js`
  - 자리 변화 시각, 감지 시각, 알림 시도·성공·실패를 기록할 형식을 정한다.
    (자리가 실제로 난 시각은 알 수 없어 **지연 상한**으로 남깁니다. 아래 3번에서 저장 위치를 정합니다 —
    지금은 `ALERTS_PATH`, 기본 `tmp/alerts.jsonl`이라 Actions 러너에서는 남지 않습니다.)
  - 새 일정 무시, 수집 실패 제외, 통합 출처 변경 시나리오를 회귀 테스트로 고정한다.
  - 완료 조건: 감지 지연과 알림 실패를 로그만 보고 계산할 수 있다.

## 2. 메타검색의 차이를 화면에서 보이기

- [x] **플랫폼 필터와 출처 배지 추가** — 현황판 첫 필터 `플랫폼`
  - 선상24·더피싱·자체 사이트를 다중 선택으로 거른다.
  - 합쳐진 출조는 원본 출처를 모두 보여주고, 각 출처의 잔여석 차이를 확인할 수 있게 한다.
  - 완료 조건: 사용자가 선상24 밖의 출조만 한 번에 볼 수 있다.
- [ ] **출조별 정확한 예약 링크 점검**
  - 각 어댑터가 사이트 메인이 아니라 가능한 가장 구체적인 일정·예약 주소를 저장한다.
  - 날짜·배·항차를 주소에 표현할 수 없는 사이트는 링크 옆에 그 한계를 표시한다.
  - 완료 조건: 대표 fixture와 실제 `peek`에서 링크가 해당 출조의 예약 과정으로 이어진다.
- [ ] **정렬 기능 추가**
  - 최신 확인순, 잔여석 적은순·많은순, 가격순을 우선 지원한다.
  - 값이 없는 행의 위치와 기본 정렬 규칙을 테스트로 고정한다.
  - 완료 조건: 필터 결과를 다시 검색하지 않고 목적에 맞게 비교할 수 있다.
- [x] **모바일 핵심 흐름 정리**
  - 작은 화면에서 필터 → 결과 확인 → 감시 → 원본 예약처 이동을 끊김 없이 수행한다.
  - 표 전체를 억지로 축소하지 말고 핵심 정보 우선순위를 정한다.
  - 완료 조건: 대표 모바일 뷰포트에서 가로 잘림 없이 핵심 흐름이 동작한다.

## 3. 취소석 레이더를 상시 서비스로 만들기

로컬 3분 감시는 검증용 기반으로 유지하되, 사용자의 PC와 브라우저가 꺼져도 감시는 계속돼야
제품의 핵심 가치가 성립합니다.

- [ ] **상시 감시 배포 구조 결정 기록 작성**
  - 국내 사이트 접근이 가능한 실행 위치, 예상 요청량, 저장소, 인증, 비용을 비교한다.
  - GitHub Actions 지연과 현재 로컬 서버의 한계를 명시한다.
  - 완료 조건: 선택한 구조와 선택하지 않은 대안의 이유가 문서에 남는다.
- [ ] **감시 목록 영속 저장과 사용자 구분 구현**
  - 서버 재시작 후에도 감시가 남고 다른 사용자의 감시 목록과 섞이지 않게 한다.
  - 임의 사용자가 관리 API를 호출하거나 다른 사용자의 목록을 읽지 못하게 한다.
  - 완료 조건: 재시작·동시 사용자·권한 실패 테스트가 통과한다.
- [ ] **상시 수집 워커 배포**
  - 플랫폼별 직렬 처리, 호스트 간격, 실패 백오프, 중복 실행 방지를 그대로 지킨다.
  - 배포 환경에서 실제 사이트를 확인하고 장애 시 이전 결과를 유지한다.
  - 완료 조건: 사용자 PC 없이 감시가 이어지고 운영 상태를 확인할 수 있다.
- [ ] **알림 채널 연결과 이력 화면 추가**
  - 감지된 변화, 원본 링크, 확인 시각을 함께 보내고 전송 실패를 재확인할 수 있게 한다.
    (기록 형식과 요약은 `core/alerts.js`·`node alerts.js`에 이미 있습니다. 여기서는 어디에 쌓을지와 화면입니다.)
  - 같은 변화의 중복 알림을 막고 사용자가 감시를 해제할 수 있게 한다.
  - 완료 조건: 테스트 알림 한 건을 생성부터 원본 링크 열기까지 추적할 수 있다.

## 4. 선상24 밖의 커버리지 늘리기

- [ ] **지원 후보 플랫폼 목록과 우선순위 작성**
  - 사용자 수요, 선상24와 겹치지 않는 출조, 공개 웹 접근성, 이용 조건, 구현 비용을 기록한다.
  - 단순히 배가 많다는 이유보다 현재 검색하지 못하는 출조가 많은 곳을 우선한다.
  - 완료 조건: 다음 어댑터 하나를 근거와 함께 선택할 수 있다.
- [ ] **후보 플랫폼의 접근 정책 확인**
  - robots, 이용약관, 공식 API·제휴 경로를 확인하고 허용 범위를 기록한다.
  - 차단 우회가 필요한 후보는 구현하지 않고 공식 연동 대상으로 돌린다.
  - 완료 조건: 수집 방법과 표시 가능한 데이터 범위가 문서에 남는다.
- [ ] **새 플랫폼 어댑터 하나 추가**
  - 최소 요청으로 날짜·배·항구·어종·상태·잔여석·원본 링크를 읽는다.
  - 실제 HTML fixture, 실패 격리, 한국시간, 중복 처리 테스트를 포함한다.
  - 완료 조건: 실제 사이트 검증과 CI를 통과하고 플랫폼별 현황에 별도로 잡힌다.
- [ ] **registry 신원 정보 정리**
  - 항구와 전화번호가 비어 합칠 수 없는 선사를 우선 검증한다.
  - 추측값은 넣지 않고 출처가 명확한 값만 기록한다.
  - 완료 조건: 품질 현황에서 신원 누락이 줄고 잘못 합쳐진 출조가 없다.
- [ ] **전체 수집 후 중복 감사 정례화**
  - 새 사이트 묶음을 추가할 때마다 `node dupes.js`를 실행한다.
  - 값이 갈린 쌍은 `--why`로 확인하고 파싱 문제와 동명이배를 구분한다.
  - 완료 조건: 같은 함대 일정표를 중복 수집하는 활성 사이트가 남지 않는다.

## 5. 사용성 검증 후 확장하기

- [ ] **실사용 검증 시나리오 작성**
  - 날짜·지역·어종으로 찾기, 선상24 밖만 보기, 마감 출조 감시, 알림에서 예약처 이동을 포함한다.
  - 완료 조건: 처음 온 사용자가 설명 없이 핵심 흐름을 끝낼 수 있는지 관찰할 수 있다.
- [ ] **기기 간 즐겨찾기·감시 동기화 검토**
  - 계정 도입의 운영 부담과 일회성 공유 코드·내보내기 방식 등을 비교한다.
  - 완료 조건: 취소석 감시에 꼭 필요한 최소 사용자 식별 방식을 선택한다.
- [ ] **개인화 추천은 사용 데이터가 생긴 뒤 재평가**
  - 검색과 감시가 충분히 사용되는지 먼저 확인한다.
  - 완료 조건: 실제 반복 패턴이 없으면 구현하지 않는다.
- [ ] **공식 데이터 연동·제휴 검토**
  - 수집 안정성이나 이용 조건이 제품 성장을 막는 플랫폼부터 협의한다.
  - 완료 조건: 제휴가 필요한 이유와 요청할 데이터 범위가 정리돼 있다.

## 베타 완료 기준

다음 흐름이 하나로 이어지면 첫 베타로 봅니다.

1. 사용자가 여러 플랫폼의 출조를 날짜·지역·어종으로 찾는다.
2. 플랫폼과 최신성을 확인하고 원하는 마감 출조를 감시한다.
3. 사용자 PC가 꺼져 있어도 감시가 계속된다.
4. 취소석이 생기면 중복 없이 알림을 받는다.
5. 알림에서 해당 출조의 원본 예약 화면으로 이동한다.
6. 운영자는 플랫폼별 커버리지·수집 실패·감지 지연·알림 실패를 확인한다.

예약·결제·환불·좌석 배정은 베타 완료 기준에 포함하지 않습니다.
