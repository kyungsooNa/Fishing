# 작업 맥락

이 파일이 이 레포의 작업 지침 원본입니다. Codex는 `AGENTS.md`를, Claude Code는
`CLAUDE.md`를 읽는데, `CLAUDE.md`는 이 파일을 가리키기만 합니다. **고칠 일이 있으면
여기만 고치세요.** 두 벌로 나눠 적으면 반드시 어긋납니다.

## 이게 뭐냐

여러 낚시 예약 사이트를 긁어 `docs/data.json` 하나로 모으고, `docs/index.html`이 그걸 읽어
표로 보여줍니다. GitHub Actions가 주기적으로 돌리고 결과를 커밋합니다. 서버는 없습니다.

제품의 정체성은 **출조 메타검색 + 취소석 레이더**입니다. 선상24 같은 예약 플랫폼을 다시
만드는 것이 아니라, 선상24·더피싱·개별 선사를 가로질러 탐색하고 자리 증가를 감시합니다.
예약·결제·환불은 원본 예약처로 연결합니다. 기능의 목표·비목표·우선순위는 `PRODUCT.md`가
기준이고, 이 파일은 그 방향을 구현할 때 지킬 기술·운영 규칙을 설명합니다.

## 구조

```
PRODUCT.md        제품 목표·비목표·판단 기준
TODO.md           제품 방향을 구현하는 작업 순서와 완료 조건
DEPLOY.md         상시 감시를 어디서 돌릴지 정한 기록. 잰 값과 고르지 않은 이유까지
PLATFORMS.md      다음에 어디를 더 볼지. 후보와 우선순위, 그렇게 판단한 근거
USERTEST.md       처음 온 사람에게 시켜볼 과제. 설명하지 않고 지켜보기 위한 목록
IDENTITY.md       누구의 감시인지 어떻게 알 것인가. 계정을 만들지 않기로 한 이유
.claude/agents/   서브에이전트. `finish`는 테스트→커밋→푸시→PR→CI→머지→최신화→재수행 담당
.claude/portable-agents/  다른 프로젝트에 복사해 쓰는 판. 자동으로 안 읽힙니다
.claude/hooks/    턴 끝 검사. ship-check가 "main 에 안 들어간 커밋"을 잡습니다
                  (`.claude/settings.json`의 `hooks.Stop`에 등록돼 있어야 돕니다)
collect.js        전체 수집 진입점 (Actions가 이걸 부릅니다)
debug.js          어댑터 고칠 때 쓰는 도구. 한 사이트만 돌려보고 표로 보여줍니다
discover.js       선사 후보를 자동으로 모으고 시험 수집합니다. 손으로 찾아 등록하는 대신
                  (`ports`는 이미 등록된 선사의 빈 항구를, `times`는 빈 출항시각을 공지에서
                  다시 읽고, `prices`는 승선료 근거 문장을 모읍니다 — 급한 곳부터 후보만 냅니다)
research.js       빈칸·선사 확인·추후 안내·만료 근거를 조사합니다. 공지 상세·첨부 링크와 확인 이력
                  수집. 실행법과 이미지까지 확인하는 작업 지시는 RESEARCH.md를 봅니다.
                  로컬 시스템 관리의 "공식 사이트 정보 조사" 버튼도 같은 명령을 실행합니다.
dupes.js          같은 일정표를 두 번 긁고 있는 사이트를 찾습니다. 수집 결과를 보고 판정합니다
metrics.js        플랫폼별 활성 사이트·성공률·출조 수 요약. 실패와 백오프 보류를 나눠 셉니다
quality.js        비어 있는 값. 합치기를 막는 신원과 화면 표시를 나눠 셉니다
alerts.js         취소석을 얼마나 빨리 잡았고 알림이 실제로 갔나. 이력을 읽어 요약합니다
robots.js         그 사이트가 받아가도 된다고 했는지. 새 플랫폼 붙이기 전 관문입니다
serve.js          docs/ 로컬 서빙 + 관리 API(/api/*). 루프백 전용, createApp()으로 테스트합니다
docs/admin.html   시스템 관리 화면. 서버가 없으면(Pages) 읽기 전용으로 떨어집니다
core/schema.js    통합 스키마 + 표기 정규화. 어댑터는 원문 텍스트만 넘기면 됩니다
core/fetcher.js   HTTP/Playwright, 호스트당 3초 간격, EUC-KR 디코딩
core/store.js     docs/data.json 읽기·쓰기
core/diff.js      이전 결과와 비교해 "새로 난 자리"만 추출
core/merge.js     같은 출조·같은 배 합치기 (신원은 이름+출항지+전화번호)
core/dupes.js     같은 함대 일정표를 내보내는 사이트 찾기. 끌 수 있는 쪽을 골라줍니다
core/quality.js   빈 칸 세기. 배 단위 신원, 어댑터별·사이트별 누락. 고칠 곳(registry/어댑터)까지
core/robots.js    robots.txt 해석(RFC 9309). 못 받은 것은 허용으로 치지 않습니다
core/platform.js  사이트 계열(선상24/더피싱/자체). 어댑터에서 자동
core/when.js      날짜 기준. 러너가 UTC여도 전부 한국시간으로 셉니다
core/ports.js     항구 좌표(지도용). sites/ports.json에서 읽습니다
core/notify.js    텔레그램/디스코드. 토큰 없으면 조용히 건너뜀. 결과는 채널별로 돌려줍니다
core/alerts.js    알림 이력(JSON Lines). 감지 시각·지연 상한·채널별 발송 결과를 남깁니다
core/monitor.js   로컬 자동 수집. 전체 60분 / 관심 출조 3분, 플랫폼별 직렬·실패 시 간격 증가
core/watchers.js  감시 목록을 사람(브라우저 토큰의 해시)마다 나눠 둡니다
core/collector-lock.js 로컬 수집 프로세스 중복 실행 방지
core/runner.js    전체 순회, 실패 격리, 지난 날짜 정리
adapters/         사이트 유형별 파서. collect(site) 하나만 내보내면 됩니다
adapters/_rows.js 출조 행 읽는 부분. 사이트에 안 얽혀 있어 어댑터들이 같이 씁니다
adapters/generic.js 자체 사이트용. 주소만 적으면 본문 표기로 읽어봅니다
sites/registry.json  ★ 사이트 목록. 보통은 여기만 고칩니다
test/             파서·수집·화면 회귀 확인 (npm test). 네트워크는 로컬 서버로 대신합니다
```

## 설계에서 지킨 것

- **파싱은 클래스명이 아니라 본문 표기로.** "운항시간 / 남은자리 / 예약마감" 같은 글자를
  기준으로 읽습니다. 템플릿이 개편돼도 표기가 남아있는 한 버팁니다.
- **요청은 최소로.** 목록형 sunsang24는 한 달치를 요청 한 번에 받습니다. 더피싱 index 방식은
  요청 한 번에 배 전부 × 4주치가 옵니다. 2주를 보려고 14번 요청하지 않습니다.
- **한 사이트가 죽어도 나머지는 산다.** 실패한 사이트는 직전 결과를 그대로 남기고
  화면 하단 "수집 상태"에 실패로 표시합니다. 화면이 갑자기 비지 않습니다.
- **"직전 결과"가 둘이면 새 쪽을 씁니다.** 로컬 서버에는 출처가 둘 있습니다 — 이 PC가 직접
  받은 것(`tmp/monitor.json`)과 Actions가 매시간 수집해 커밋한 것(`docs/data.json`, run.bat이
  받아옵니다). 로컬 수집이 실패한 선사를 무조건 로컬 직전 결과로 덮으면, 받아온 값이 더
  새것인데도 그 배만 현황판에서 시간이 멈춥니다. 헌터호가 실제로 나흘 동안 그랬습니다 —
  사이트도 Actions 수집도 멀쩡했고 로컬에서만 실패했습니다. 그래서 **실패한 선사는 둘 중
  새 쪽**을 싣고(`core/monitor.js`의 `stalled`), 받아온 쪽을 쓸 때도 이 PC의 수집이 깨진
  것은 `localError`로 남겨 "수집 상태"에 적습니다 — 그 선사는 3분 감시가 안 돕니다.
  성공한 로컬 수집은 따지지 않습니다(방금 직접 확인한 값이고 알림도 그 값으로 갑니다).
  같은 이유로 `docs/data.json`은 켤 때 한 번이 아니라 **파일이 바뀌면 다시 읽습니다**
  (6.7MB라 mtime·크기가 달라졌을 때만).
- **알림 이력도 사람마다 나눕니다.** 기록에 "누구의 감시가 잡았나"(`watchers`, 이미 해시)를
  남겨서 `/api/alerts`가 그 사람 것만 돌려줍니다. 감시 목록을 나눠 놓고 이력을 다 보여주면
  나눈 뜻이 없습니다. 주인이 안 적힌 옛 기록은 아무에게도 안 보입니다.
- **같은 소식을 두 번 울리지 않습니다.** 관심 출조는 3분마다 봅니다. 잔여석이 3→5로 늘었다가
  누가 예약해 3이 되고 또 취소돼 5가 되면 화면에는 변화가 두 번이지만 사람에게는 같은
  소식입니다. 몇 번 헛울리면 알림을 통째로 꺼버리고, 그러면 정작 필요한 알림도 같이 잃습니다.
  그래서 "같은 출조·같은 이유·같은 자리 수"는 6시간 안에 다시 안 보냅니다
  (`core/alerts.js`의 `dropRepeats`). 자리가 더 늘어난 것(5→8)은 다른 소식이라 보냅니다.
  **보낸 것으로 치는 기준은 "보냈다"가 아니라 "보내려 했다"입니다** — 채널이 죽어 실패한 것을
  3분 뒤에 또 시도하면 채널이 살아나는 순간 밀린 알림이 한꺼번에 옵니다. 그리고 보낸 기록은
  **알린 직후에 저장합니다**. 수집 직후의 저장은 이 기록이 생기기 전이라, 알리고 바로 죽으면
  다시 떴을 때 또 보냅니다(테스트가 이걸 잡았습니다).
- **알림은 값어치 있는 것만.** 취소석과 자리 늘어남만 알립니다. 새로 올라온 일정은
  아무도 예약 안 한 게 당연해서 알리지 않고, 수집 실패한 사이트는 비교에서 뺍니다.
- **같은 배를 합칠 땐 이름만 믿지 않습니다.** 다른 지역에 같은 이름의 배가 있어서,
  이름·출항지·전화번호가 셋 다 있고 셋 다 같을 때만 합칩니다(`core/merge.js`).
  하나라도 비면 안 합칩니다 — 합쳐서 틀리는 것보다 두 줄이 낫습니다.
  세 값은 전부 registry에서 오므로 합칠지 말지는 사람이 정하는 셈입니다.
- **별점은 표에서 바로 매기되, 두 번 눌러야 바뀝니다.** 배별 별점(개인 리뷰)은 현황판과 시스템
  관리 화면 **둘 다에서** 매깁니다(`localStorage`의 `fishing:boat-ratings`). 오래 현황판을 읽기
  전용으로 뒀던 이유는 표를 빠르게 훑다가 잘못 눌러 점수가 조용히 바뀌는 것이었는데, 그
  대가로 **매기러 다른 화면까지 가야 해서 아무도 안 매겼습니다**(실제로 "별점이 안 보인다"는
  말이 나왔고, 저장된 별점이 0개였습니다). 지금은 오클릭을 화면 이동이 아니라 **한 번 더
  누르게** 해서 막습니다 — 별을 누르면 고르는 칸(`ratepop`)이 열릴 뿐이고 거기서 점수를
  골라야 저장됩니다. 고르는 칸은 화면에 **하나만** 만들어 옮겨 씁니다(줄마다 만들면 한 쪽에
  1200개가 생깁니다). 표는 다시 그리지 않고 **별만 고쳐 그립니다**(`applyRate` — 즐겨찾기와
  같은 이유로 178ms가 걸립니다). 저장이 막히면 **없던 일로 되돌립니다** — 화면만 바뀌면
  매긴 줄 알고 창을 닫습니다. 안 매긴 배는 `☆` 하나만 답니다(모든 줄에 `☆☆☆☆☆`가 깔리면
  표가 안 읽힙니다).
  **그 `☆`는 눈에 보여야 합니다.** 처음에 테두리색(`--line`)으로 뒀더니 흰 배경에서 안 보여서
  "별점이 안 보인다"는 말이 또 나왔습니다 — 매길 수 있다는 것 자체를 모릅니다. 지금은
  `--muted`로 또렷하게 두고(즐겨찾기 별도 같은 이유로 같이 고쳤습니다), 색은 매긴 별에만
  줍니다 — 같은 색이면 안 매긴 배가 매긴 것처럼 보입니다.
  즐겨찾기처럼 서버로 안 보내는 개인 기록이라 읽기 전용 모드(Pages)에서도 매길 수 있습니다.
  키는 `사이트 id|배 이름`이라 같은 선사의 배마다 따로 매기고 다른 선사의 동명 배도 섞지 않습니다.
  합쳐진 출조는 어느 출처에서 매긴 점수든 한 번만 보여줍니다. 형식(1~5점 정수, 안 매긴 배는 키
  자체가 없음)을 바꾸려면 `docs/admin.html`과
  `docs/index.html`을 같이 고쳐야 합니다 — 즐겨찾기가 그렇듯 두 화면에 같은 코드가 있습니다.
  **이름에만 적힌 오전·오후는 수집이 구분 칸에 채웁니다.** 몬스터호는 예약판에 시각도 구분도
  없어서(`session: null`) 그대로 두면 구분 칸이 비고 오전·오후 필터에서 통째로 빠집니다 —
  아는 값인데 못 거르는 것입니다. `core/schema.js`의 `sessionFromName`이 그 낱말을 읽어
  `sessionOf`가 못 센 자리를 메웁니다. **예약판에 시각이 있으면 언제나 시각이 먼저이고**
  (이름은 빈 칸을 메울 뿐), `hours`는 지어내지 않습니다 — 길이는 시각이 있어야 셉니다.
  화면이 아니라 수집에서 채우는 이유는 알림·품질 집계·API가 같은 값을 봐야 해서입니다.
  **다음 수집이 돌아야 화면에 반영됩니다**(registry를 고쳤을 때와 같습니다).
  **오전배·오후배는 한 배로 봅니다.** 같은 배를 오전·오후로 나눠 올리는 곳이 있습니다 —
  오이도 몬스터호는 `몬스터호 (오전배)`, 우리바다호·52fish는 괄호 없이 `우리바다호 오전배`
  입니다(2026-09-16 수집본에 이 셋뿐입니다). 사람에게는 한 배라 즐겨찾기와 별점은 한 번만
  매기고 두 줄에 같이 보입니다(`boatId`가 `SESSION_TAIL`을 뗍니다 — 즐겨찾기 키와 별점 키가
  같이 씁니다). **출조 자체는 안 합칩니다** — 출항 시각이 다르면 다른 출조이고 자리도 따로
  셉니다. 낱말은 **끝에 붙고 앞에 빈칸이나 괄호가 있을 때만** 뗍니다(`만선호오전배`처럼 붙여
  쓴 이름과 `종일호` 같은 배 이름을 건드리지 않으려는 것입니다 — 한글에 `\b`가 없어 생기는
  그 함정입니다). 떼고 나서 빈 이름이 되면 원래 이름을 씁니다. **규칙은 저장 형식의 일부라
  두 화면이 같아야 합니다** — 갈리면 관리 화면에서 매긴 별점을 현황판이 못 찾습니다
  (`test/admin.test.js`가 두 줄을 글자까지 맞춰 봅니다). 관리 화면의 별점 칸도 줄인 이름으로
  묶어 한 줄만 보여줍니다 — 키가 하나뿐이라 두 줄로 두면 같은 점수가 두 번 나옵니다.
  **별점의 배 이름은 수집이 읽은 이름(`trip.boat`)이어야 합니다.** registry의 `boats`에 사람이
  적어둔 표기는 수집 결과와 갈릴 수 있습니다 — `raraho`는 registry가 `라라호`, 수집은
  `오천항 라라호`입니다(2026-09-16 기준 registry 배 430척 중 32척이 그렇습니다). 관리 화면은
  둘을 합쳐 보여주므로 registry 표기에 별을 주면 **현황판에서 영원히 안 보입니다.** 그래서
  못 읽는 이름에는 별을 아예 안 답니다(`docs/admin.html`의 `boatRatesCell` — `OBSERVED`가
  수집이 읽은 이름입니다). **비슷하다고 합치지는 않습니다** — `마성호`와 `뉴마성호`처럼 한쪽이
  다른 쪽에 들어가는 다른 배가 있습니다. 이름이 다른 것(`수집 이름 아님`)과 그 선사의 출조가
  아예 없는 것(`수집 결과 없음`)은 고칠 곳이 달라서 다르게 적습니다.
  **표에서는 별점을 배 칸에 답니다**(`docs/index.html`의 `i === 4`). 선사 칸에 달아뒀을 때는
  매긴 사람이 표에서 자기 별점을 못 찾았습니다 — 별을 준 이름은 배 이름인데 배와 선사는
  다른 칸입니다. 이름만 잘리고 별은 남도록 이름은 `boatname`으로 감쌉니다.
  **그 규칙이 생기기 전에 매긴 별점은 버리지 않고 옮깁니다.** registry 표기로 저장된 점수는
  현황판에 영영 안 뜨는데, 매긴 사람에게는 "별점이 안 보인다"로만 보입니다. 관리 화면의
  **현황판에 안 보이는 별점** 칸이 그런 키를 모아 수집 이름으로 옮기거나 지웁니다
  (`renderLostRates`). **이름은 우리가 짐작하지 않습니다** — 고르는 것은 사람이고, 옮기기는
  지우기와 저장이 한 번에 일어나야 합니다(중간에 저장이 막히면 점수를 잃습니다).
  **매긴 별점은 걸러 보고 정렬할 수 있습니다** — 현황판의 `★ 별점 준 배만`과 `내 별점 높은순`.
  둘 다 **1만 건을 훑는 자리**라 별점을 출조마다 다시 재면 안 됩니다(`rateForTrip`은 호출마다
  배열과 Set을 만듭니다 — `Intl.DateTimeFormat`으로 3초를 태운 그 자리입니다). `rateOfTrip`이
  출조마다 한 번만 재고 `RATE_CACHE`(WeakMap)에 기억하며, **`RATES`를 다시 읽는 곳에서는
  반드시 `clearRateCache()`를 같이 부릅니다**(30초 갱신, 다른 탭의 `storage` 이벤트) — 안 버리면
  관리 화면에서 바꾼 별점이 이 탭에 안 나타납니다. 정렬에서 안 매긴 배는 0이 아니라 `null`을
  줍니다(`byValue`의 "값 없는 행은 방향과 상관없이 뒤로"). 빈 목록 안내는 즐겨찾기처럼 셋으로
  갈라 말합니다(`ratedEmptyMessage`) — 안 매김 / 매겼는데 목록에 없음(이름이 갈린 경우라
  위의 되살리기 칸으로 안내) / 다른 필터에 걸림.
- **현황판 표는 일주일씩 끊어 봅니다.** 3주치가 한 화면에 수천 줄로 쏟아지던 것을
  날짜 기준 7일씩 나눴습니다(`docs/index.html`의 `paginate`). 달력 주가 아니라 **출조가 있는
  날 7개**입니다 — 빈 날까지 세면 어떤 쪽은 텅 빕니다. 자르는 단위가 날짜라 한 날의 출조가
  두 쪽에 갈리지 않습니다. 필터를 바꾸면 첫 주로 돌아가고(`filtered()`), 별을 누르거나
  감시를 걸 때는 보던 쪽에 그대로 있습니다. 지도는 나누지 않습니다 — 쪽마다 마커가 달라지면
  "어디에 배가 뜨는지" 보는 화면이 못 됩니다. 대신 힌트에 그렇게 적어뒀습니다.
- **거르는 경로에서 값비싼 객체를 만들지 마세요.** 출조가 1만 건이라 `visibleTrips()`의
  필터 안은 건당 1만 번 도는 자리입니다. 거기서 `Intl.DateTimeFormat`을 새로 만들다가
  거르기 한 번에 3초가 걸렸습니다(`showTripByTime` → `kstParts`). 포맷터는 `KST_FORMAT`으로
  한 번만 만들고, 기준 시각도 `visibleTrips()` 진입 때 한 번만 구해 넘깁니다.
  같은 이유로 표와 지도는 `refresh()`가 **한 번 거른 결과를 나눠 씁니다** — 각자 거르면
  같은 1만 건을 두 번 훑습니다. 검색어는 150ms 디바운스에 한글 조합(`compositionstart`/
  `compositionend`) 중에는 그리지 않습니다 — 조합 중에도 `input`이 뜹니다.
  행은 `DocumentFragment`에 모아 한 번에 붙입니다(한 쪽이 3천 줄 · 4만 노드).

- **비싼 것은 기다리게 두고, 싼 것을 먼저 보여주세요.** 디바운스는 표를 덜 그리게 하지만
  치는 동안 화면이 **아무 반응이 없습니다.** 그래서 "검색이 느리다"는 말이 나왔습니다 —
  실제로 느린 것은 검색이 아니라 표 다시 그리기입니다(CDP로 CPU를 6배 늦춰 재면 672줄에
  449ms, 거르기는 46ms). 고칠 수 있는 쪽은 값이 다른 일을 나누는 것입니다: **배 이름
  자동완성**(`docs/index.html`의 `boatSuggestions`)은 이름 439개만 훑어 같은 조건에서
  3~4ms라, 디바운스 없이 **글자마다, 한글을 조합하는 중에도** 보여줍니다. 표는 그대로
  150ms 뒤에 한 번 그립니다. 그리고 후보를 고르면 대개 다 치기 전에 끝나서 표를 그리는
  횟수 자체가 줍니다(고를 때는 디바운스를 안 기다리고 그 자리에서 겁니다).
  **이름 목록은 수집을 받을 때 한 번만 셉니다**(`buildBoatNames`) — 8015건에 같은 이름이
  수십 번씩 들어 있어서, 글자마다 다시 세면 자동완성이 표만큼 비싸집니다. 목록의 이름은
  **수집이 읽은 이름 그대로**여야 합니다(`trip.boat`) — registry 표기로 만들면 골라도 안
  걸리는 이름이 섞입니다(별점이 안 보이던 것과 같은 함정). 키보드로 고를 때 Enter·화살표는
  **한글 조합 중이면 IME 것입니다**(`e.isComposing`) — 가로채면 글자가 깨집니다. 누르는 것은
  `click`이 아니라 `mousedown`으로 잡습니다 — `click`은 `blur` 뒤에 와서 칸이 이미 닫혀 있습니다.

- **버튼 하나 바뀌자고 표를 다시 그리지 마세요.** 별(즐겨찾기)과 감시 버튼은 눌러도
  표에 나오는 줄이 그대로입니다. 그런데 `refresh()`·`render()`를 통째로 불러서 별 하나
  누르는 데 178ms씩 멈췄습니다(한 쪽이 행 1200개 · 노드 2만 개). `render()`가 그리면서
  `ROW_BUTTONS`에 버튼을 적어두고, 누를 때는 `paintStar`/`paintWatch`로 그 버튼만
  고쳐 그립니다 — 1.1ms. **예외는 "즐겨찾기만"을 켠 때**입니다. 그때는 별을 빼면 그 줄이
  사라져야 해서 전체를 다시 그립니다. 같은 배가 여러 날에 여러 줄로 나오므로(키가
  이름+출항지) 키가 같은 별은 다 같이 고쳐야 합니다. 감시 버튼은 표를 다시 안 그리니
  `disabled`를 직접 풀어줘야 합니다.
- **표 그리는 값은 재 보고 줄이세요 — 눈대중은 두 번 틀렸습니다.** 남은 `render()`
  120ms는 `createElement`·`append`가 대부분이고(CDP 프로파일), 짐작했던 두 가지는
  헛다리였습니다: 행마다 리스너를 다는 값은 1200개에 **0.6ms**라 이벤트 위임은 소용없고,
  `cloneNode`로 견본 행을 찍어내는 것도 **6~9%**뿐입니다. 더 줄이려면 방법은 하나,
  **한 번에 그리는 행을 줄이는 것**(쪽을 더 잘게 나누거나 보이는 만큼만 그리기)입니다.

- **관심 없는 것은 숨기되, 과하게 숨기지 마세요.** 선사·배·항구·지역을 내 화면에서만
  안 보이게 합니다(`docs/index.html`의 `MUTES`, `localStorage`의 `fishing:muted`) —
  즐겨찾기·별점과 같은 급의 개인 기록이라 읽기 전용(Pages)에서도 됩니다. **수집을 끄는
  것(registry의 `enabled:false`)과 다릅니다** — 그쪽은 모두에게 영향을 주고 그 선사 자리를
  아예 못 잡게 만듭니다. 여기서 제일 조심할 것은 **자리를 놓치는 쪽으로 기우는 것**입니다:
  합쳐진 줄은 **출처가 모두 숨긴 선사일 때만** 숨깁니다(`isMuted`) — 한 곳이라도 안 숨긴
  사이트에서 올라온 자리는 잡을 수 있습니다. 같은 이유로 배는 즐겨찾기와 **같은 키**
  (`이름|항구`)라 다른 지역의 동명이배까지 숨지 않고, 오전·오후로 나뉜 배는 한 번에 숨습니다.
  **선사는 id가 아니라 이름으로** 숨깁니다 — 화면이 registry를 못 읽어 표·필터가 전부 이름
  기준이고(`t.siteName`), 표기가 바뀌면 숨김이 풀려 **다시 보이는 쪽으로** 틀립니다.
  거는 자리는 **필터 메뉴의 `✕`**(선사·항구·지역)와 **별점 칸의 "숨기기"**(배)입니다 —
  표에 버튼을 더 달면 한 쪽 1200줄에 1200개가 늡니다(위의 "버튼 하나 바뀌자고" 항목).
  **푸는 길이 거는 길보다 먼저입니다** — 숨긴 것이 있으면 `숨긴 것 N개` 패널이 열리고 거기서
  하나씩·한꺼번에 되돌립니다. 못 풀면 "배가 안 뜬다"로 보이고 그건 고장과 구별이 안 됩니다.
  저장이 막히면 **없던 일로 되돌립니다**(별점과 같은 이유 — 화면만 바뀌면 숨긴 줄 압니다).
- **감시 목록은 사람마다 다르고, 계정은 만들지 않습니다.** 브라우저가 무작위 토큰 하나를 만들어
  보관하고(`localStorage`의 `fishing:watcher`), 서버는 **그 토큰의 해시**를 열쇠로 목록을 나눠
  둡니다(`core/watchers.js`). 해시만 두는 이유는 저장 파일이 새더라도 남의 감시를 대신 걸거나
  지울 수 없게 하려는 것입니다. 즐겨찾기·별점과 같은 급의 개인 기록이라 로그인을 시키면
  아무도 안 씁니다. **응답에는 그 사람 것만 싣습니다** — 목록만 봐도 누가 어느 배를 노리는지
  드러납니다. 반면 **수집 주기는 모두의 목록을 합쳐** 정합니다(한 번 받아 다 같이 나눠 씁니다).
  상한 50개도 사람마다 셉니다 — 한 벌로 세면 남이 채워서 내가 못 겁니다.
  사용자 구분이 없던 시절의 목록(`watches` 한 벌)은 주인을 모르므로 버리지도 아무에게나 주지도
  않고, **로컬 요청에 한해 한 번만** 처음 붙은 사람에게 넘깁니다(`adopt`). 그동안에도 감시는
  계속합니다 — 주인을 못 찾았다고 멈추면 그 사이 자리를 놓칩니다.
- **정렬은 하루 안에서만 겁니다.** 표는 날짜로 끊어 보고(`dayhead`) 쪽도 날짜로 나누기 때문에
  (`paginate`), 잔여석순으로 통째로 섞으면 날짜 머리글이 줄마다 나오고 쪽 나누기가 무너집니다.
  사람도 "언제 갈지"를 먼저 정하고 그 날 배를 고릅니다. 그래서 비교 기준은 날짜 다음입니다.
  **값이 없는 행은 방향과 상관없이 뒤로** 갑니다 — 적은순이라고 모르는 잔여석을 0으로 쳐서
  앞에 세우면 못 읽은 배가 "자리 없는 배"로 보입니다. 두 값이 다 없으면 0을 돌려줘 수집이
  정렬해 온 순서(`core/runner.js`의 `sortTrips`)에 맡깁니다 — `Array.sort`는 안정 정렬입니다.
  **하루 안의 차례는 즐겨찾기 → 별점 → 빈자리 → 고른 기준입니다**(`docs/index.html`의
  `byFav`·`byRate`·`byVacancy`). 하루에 수백 줄이 나오는데 어느 배가 내가 눈여겨본 배인지는
  표를 훑어야 알았습니다 — 즐겨찾기와 별점은 그걸 **이미 적어둔 값**이라 자리 여부보다
  앞에 둡니다. 안 매긴 배는 `byValue` 규칙대로 뒤로 가고(0점이 아니라 값 없음), 별점이 낮은
  배도 안 매긴 배보다는 위로 옵니다 — 내가 아는 배이고 몇 점인지는 표에 보입니다.
  그다음이 빈자리입니다. 잡을 수 있는 자리를 찾는 화면이라 마감·휴항된 배가 그 날 앞줄을
  차지하면 안 됩니다 — "빈자리만"을 끄고 하루를 통째로 볼 때 드러납니다(내가 찍어둔 배는
  예외입니다. 마감이어도 "오늘 떴는지"를 보고 싶습니다).
  **별이나 점수를 눌러도 그 줄은 제자리에 있습니다** — 누르는 순간 줄이 튀어 올라가면 다음에
  누를 것을 다시 찾아야 하고, 표를 다시 그리는 값도 178ms입니다. 다음에 다시 그릴 때
  (필터를 바꾸거나 새 수집이 올 때) 자리를 잡습니다. 기준은 그 필터와 같은 `open`·`few`입니다(갈리면 체크를
  껐다 켤 때 같은 줄이 위아래로 튑니다). `unknown`은 올리지 않습니다 — 자리가 있는지 모르는
  배로 첫 화면을 덮느니 마감된 줄과 같이 뒤에서 수집 순서를 지키는 편이 낫습니다.
- **전화번호는 관리 화면에서 적고 현황판에서 겁니다.** 값을 고치는 곳은 여전히 시스템 관리
  한 군데지만(registry의 `phone`), 보여주는 곳은 현황판입니다 — 자리를 찾은 사람이 배 이름을
  들고 다시 검색하게 두면 취소석을 놓칩니다. 선사 칸 밑에 `tel:` 한 줄로 답니다
  (`docs/index.html`의 `phoneLink`). **칸을 새로 만들지 않은 이유**는 표의 자연 폭이 그만큼
  늘어 오른쪽 칸이 잘리기 때문입니다(위의 `nowrap` 항목과 같은 이야기). 표기는 제각각이라
  (`010-1234-5678` / `010.1234.5678`) 보여주는 글자는 적어둔 그대로 두고 `tel:`에만 숫자를
  넣습니다. 합쳐진 줄에도 번호는 하나입니다 — 이름·출항지·전화가 셋 다 같아야 합치니까요.
- **링크는 받아온 주소가 아니라 그 출조의 주소입니다.** 더피싱 상세는 한 요청에 일주일치가
  와서(`windowDays`), 받아온 페이지 주소를 그대로 붙이면 9월 8일 출조를 눌렀는데 9월 7일
  예약판이 열립니다. 주소에 날짜를 넣을 수 있는 사이트는 출조마다 제 날짜로 답니다
  (`adapters/thefishing.js`의 `dayUrl`). 못 넣는 사이트(선상24 함대 일정표, 의지호 월 달력)는
  그대로 두고 **화면이 링크 옆에 `일정표`라고 적어** 날짜를 직접 찾아야 함을 알립니다.
  출조의 `urlDated`가 그 표시입니다 — 기본값은 false, 즉 "날짜로 간다고 말하지 않음"입니다.
- **화면은 registry를 못 읽습니다.** GitHub Pages에는 `docs/`만 올라가서, 어느 계열에서 온
  출조인지는 `data.json`의 사이트 상태에 실린 `platform` 표기로만 압니다(`core/runner.js`의
  `meta()`). 현황판의 플랫폼 필터·출처 배지가 그 값을 씁니다 — registry에 계열을 손으로
  적어 고쳐도(`"platform": "자체"`) **다음 수집이 돌아야 화면에 반영됩니다.**
  `(상세)`는 우리가 더피싱을 어떻게 긁는지일 뿐이라 화면에서는 떼고 보여줍니다.
- **합친 줄은 출처별 잔여석을 같이 보여줍니다.** 합칠 때 잔여석은 큰 쪽을 씁니다
  (`core/merge.js`) — 어디든 자리가 있으면 잡을 수 있으니까요. 그런데 그러면 **어느
  사이트로 가야 그 자리가 있는지**를 화면에서 알 수 없습니다. 그래서 출처마다 숫자가
  갈릴 때만 각 링크 옆에 그 사이트 기준 잔여석을 적습니다(같으면 군더더기라 안 적습니다).
- **자리가 실제로 언제 났는지는 모릅니다.** 사이트는 지금 잔여석만 보여줍니다. 우리가 아는 건
  "직전에 봤을 때는 없었고 이번에 보니 있더라"는 구간뿐이라, 이력에 남기는 건 지연이 아니라
  **지연 상한**입니다(`core/alerts.js`의 `delayMaxMs = at - since`). 3분마다 보면 상한이 3분,
  한 시간마다면 한 시간 — 감시 주기를 줄일지 말지를 이 숫자로 정합니다. 실제 지연을 아는 척하는
  필드는 두지 마세요. 직전 확인 시각을 모르면 `null`로 두고 분포에서도 뺍니다.
- **알림은 보냈든 못 보냈든 남깁니다.** 막힌 알림이야말로 남아야 하는 기록입니다. 채널이 없어
  아예 못 보낸 것(`no-credentials`)과 보내다 실패한 것은 다르게 셉니다 — 앞은 설정 문제고
  뒤는 채널 문제라 고칠 곳이 다릅니다.
- **빈 칸이라고 다 같은 빈 칸이 아닙니다.** 승선료가 없으면 표의 그 칸이 비고 끝이지만,
  항구·전화번호가 없으면 다른 사이트의 같은 배와 못 합쳐 **같은 출조가 두 줄로 뜹니다**
  (`core/merge.js`). 그래서 `core/quality.js`는 신원과 표시를 나눠 세고, 신원은 사이트가
  아니라 **배 단위**로 봅니다(`boats`에 배별 항구를 적어둔 곳이 있어서 사이트로 세면
  반쯤 채워진 곳이 통째로 잡힙니다). 그리고 **이름이 겹치는 배가 없으면 항구가 비어도
  지금 손해는 없습니다** — 실제로 두 줄로 뜨고 있는 배만 "막힘"으로 셉니다.
- **"배가 안 뜬다"는 잔여석 숫자보다 셉니다. 그리고 그 말이 배 행에 없을 수 있습니다.**
  무창포 대진피싱은 배정비일에도 배 행을 `예약하기 / 남은자리 20명`으로 그대로 두고,
  아래 **취소자 행에만** "배정비일"과 "배 점검으로 예약을 받지 않습니다"를 적습니다. 그래서
  두 곳이 같이 틀렸습니다 — 그 행은 좌석도 예약 표기도 없어 "상시 공지" 관문에 걸려
  버려지고(정비 문구가 수집 자체에 안 남습니다), 설령 남았어도 `toStatus`는 잔여석 숫자가
  있으면 그 숫자를 믿어 `OFF_WORDS`에 없는 정비·점검을 못 이깁니다. 정비를
  `CLOSED_WORDS`에 넣어도 자리가 남은 날은 그대로 예약가능입니다. 그래서 **정비·점검은
  `OFF_PATTERNS`**(잔여석을 뒤집는 쪽)에 두고, **배가 아닌 행(취소자·대기자·공지)의
  "남은자리" 칸이 "배가 안 뜬다"고 말하면 그 날 쪽지로 모아 같은 날 배 행의 상태 표기에
  얹습니다**(`adapters/thefishing.js`의 `dayNotes`). 판정은 `core/schema.js` 한 곳에서만
  합니다 — 어댑터는 그 칸을 `toStatus`에 물어봅니다. 짚어둘 것 셋:
  **(1) 본문이 아니라 상태 표기에만 얹습니다** — 쪽지를 본문에 섞으면 공지의 "03시30분"이
  출항시각이 되고 "남은자리 배정비일"이 잔여석 자리를 차지합니다. **(2) 앞에 붙입니다** —
  상태 표기는 200자에서 잘립니다. **(3) 배가 여럿인 날은 쪽지에 배 이름이 적혀 있을 때만
  얹습니다** — 멀쩡한 배를 휴항으로 적는 쪽이 정비일을 놓치는 것보다 나쁩니다.
  `취소자` 칸의 "마감"은 **취소석이 없다**는 뜻이라 쪽지가 아닙니다.
  한글에는 `\b`가 없으니 정비·점검은 **"배·선박"이 앞에 붙은 것만** 봅니다 —
  "장비점검"·"채비정비"가 그대로 통과합니다(항구 후보에서 "공지사항"이 통과한 그 함정).
  **여기서 이건 가정이 아니라 잰 값입니다**: 선상24 예약판은 모든 행에 상태 범례를 같이
  적어서("예약완료 입금대기 취소대기 취소완료 예약대기 출조대기 **점검일**"), 2026-09-14
  수집본에 "점검" 글자가 든 행이 129건인데 그중 127건이 이 범례였습니다. 낱말을 안 끊고
  찾으면 그 판이 통째로 휴항이 됩니다 — 잡을 수 있는 자리가 화면에서 사라지는 쪽이라
  놓치는 것보다 나쁩니다. 범례는 `test/parse.test.js`가 고정해 둡니다.
- **선사 공지에서 옮긴 출항시각은 유효기간과 출처를 달고 삽니다.** 예약판에 시각이 없는
  선사는 홈페이지 공지가 유일한 근거라, 거기서 읽은 값은 `timeGuide`에 **언제까지 유효한지와
  어디서 봤는지, 원문 인용까지** 적습니다(`core/schema.js`의 `makeTrip`). 시즌마다 바뀌는
  값이라 근거가 없으면 다음 시즌에 틀린 시각이 조용히 남습니다. **어종은 공지가 어종을 가릴
  때만 적습니다** — 52fish는 "오전배 : 5시 출항"처럼 배로만 갈리고 바다사랑호는 어종 없이
  "05시 30분 출항"이라고만 적어서, 어종을 요구하면 공지에 없는 어종을 우리가 지어내야 합니다.
  안 적으면 그 선사(배별로 적었으면 그 배)의 모든 출조에 걸립니다. 유효기간은 그래도 필수입니다.
  **예약판에 그 출조의 시각이 따로 있으면 언제나 그 값이 먼저입니다** — 공지는 빈 칸을 메울
  뿐이고, 메운 줄은 `timeSource: "notice"`로 표시가 남습니다.
- **어종마다 시각이 갈리면 `timeGuides` 배열로 여러 줄을 적습니다**(`priceGuides`와 같은 모양).
  한 배가 어종마다 다른 시각으로 뜨는 곳이 있습니다 — 나라2호 공지는 한치·갈치 13시,
  먼바다 갑오징어·참돔 04시, 내만 갑오징어·문어·쭈꾸미 04시 30분입니다. `timeGuide` 하나로는
  그중 하나밖에 못 적어서 나머지 어종이 빈 채로 남습니다. **앞에서부터 조건에 맞는 첫 줄**을
  쓰니 구체적인 줄을 앞에 적고, 어종을 안 적은 줄을 맨 뒤에 두면 나머지를 받습니다. 한 줄이면
  예전처럼 `timeGuide` 하나로 적어도 됩니다. **배에 시각을 적어뒀으면 사이트 공통은 안 봅니다** —
  뒤로 새어 들어오면 그 배만 예외인 줄 알았는데 나머지 어종에 엉뚱한 시각이 붙습니다.
  유효기간·출처 규칙은 줄마다 그대로 지킵니다(기간이 없는 줄은 건너뜁니다).
- **공지에 입항시각이 적혀 있으면 그것도 씁니다.** 오래 `timeGuide.departAt`만 읽고
  `returnAt`은 읽고도 버렸습니다. 그러면 "※출항시간:05 시 ※입항시간:15 시~16 시"라고 둘 다
  적어둔 정원낚시 같은 곳이 **10시간짜리 종일배인데 오전배로** 잡힙니다 — `sessionOf`가 길이를
  모르면 시작 시각만 보기 때문입니다. 입항이 범위면(`15시~16시`) **이른 쪽**을 적고 원문은
  `note`에 남깁니다 — 스키마에 입항 범위 필드가 없고, 늦은 쪽을 적으면 없는 운항시간을
  지어내는 셈입니다. 예약판에 적힌 값이 언제나 먼저인 것은 출항시각과 같습니다.
- **집결시각과 출항 범위는 운항 종료시각이 아닙니다.** `04시30분까지 매장 도착`처럼 현재
  예약판에 붙은 안내는 `meetingAt`으로 읽어 운항 칸에 `04:30까지 도착`이라고 보여줍니다.
  `05시에서 05시30분 사이 출항`은 `departAt`과 `departThrough`로 나눠
  `05:00~05:30 출항`이라고 씁니다. 둘째 시각을 `returnAt`에 넣으면 30분 운항처럼 보입니다.
  공지의 `timeGuide`도 같은 필드를 쓸 수 있지만, 출처·적용 기간 규칙은 그대로 지킵니다.
- **빈 칸을 "고칠 곳"까지 말하려면 실제로 어디서 오는 값인지 봐야 합니다.** 승선료를 오래
  "어댑터"로 세고 있었는데, 승선료를 넘기는 어댑터는 하나도 없습니다 — registry의
  `priceGuides`·`prices`·`price`에서만 옵니다(`core/schema.js`의 `pickPrice`). 그동안 "파서를 고치면
  채워진다"고 적혀 있었지만 고칠 파서가 없었습니다. 출항시각은 반대 함정입니다. 어댑터가
  읽는 값이 맞는데 **예약판에 아예 안 적힌 사이트가 91곳(2,827건)**이라, 거기는 파서를
  고쳐도 안 채워지고 선사 공지에서 확인해 registry의 `timeGuide`에 적어야 합니다. 그래서
  `core/quality.js`의 `timeGaps`가 사이트를 전부 있음 / 섞임(배마다·날짜마다) / 전부 없음으로
  가릅니다 — **파서를 의심할 곳은 "같은 배인데 날짜마다 갈리는 곳"뿐입니다**(15곳 468건).
  그마저 열 곳을 열어보니 전부 그 날 예약판에 `운항시간` 줄이 없었습니다(선상24는 배·날짜마다
  비고, 더피싱 winner는 아예 "출항시간등 안내문자는 전날 점심때 보내드립니다"라고 적습니다).
  **출항시각은 지금 파서로 채울 게 사실상 없습니다** — 화면이 "선사 확인"이라고 적는 게 맞고,
  채우려면 선사 공지를 사람이 확인해 `timeGuide`에 적어야 합니다.
- **보류는 실패가 아닙니다.** 백오프로 건너뛴 사이트를 실패로 세면 "더피싱 111곳 전부 실패"가
  되어 정말 죽은 곳이 그 안에 묻힙니다. 그렇다고 성공으로 볼 수도 없습니다 — 화면에 실린
  값은 몇 시간 전 것입니다. `core/metrics.js`는 성공·실패·보류·미수집 넷으로 나눠 세고,
  시각도 "언제 시도했나"가 아니라 **지금 화면에 실린 값이 언제 것인가**(실패한 곳은
  `keptFrom`)로 셉니다. 시도 시각으로 세면 사흘 묵은 값이 "15분 이내"로 잡힙니다.
- **timeout 백오프는 지수로 늘립니다.** 한 번 timeout 났다고 상한(`TIMEOUT_BACKOFF_HOURS`,
  Actions에서 6시간)을 통째로 쉬면, 매시간 도는 수집이 5~6회 헛돕니다. 실제로 더피싱 111곳이
  하루 두 번씩 한꺼번에 죽었는데 **재시도하면 그대로 살아났습니다** — 잠깐 막혔던 것뿐입니다.
  그래서 처음엔 1시간만 쉬고 연달아 또 timeout이면 2·4시간으로 늘려 상한에서 멈춥니다
  (`core/runner.js`의 `backoffMsFor`). 연속 횟수는 `timeoutStreak`로 data.json에 남고,
  성공하거나 timeout이 아닌 이유로 실패하면 0부터 다시 셉니다. `TIMEOUT_BACKOFF_HOURS`는
  이제 고정값이 아니라 **상한**입니다.
- **더피싱이 연속으로 막히면 나머지는 보류합니다.** 자체 도메인을 쓰는 선사도 실제로는 같은
  예약 백엔드라 `core/runner.js`가 더피싱 전체를 한 무리로 둡니다. 세 곳이 연달아 timeout이면
  공통 장애로 보고 나머지는 요청하지 않은 채 직전 결과를 유지합니다(`platform-timeout`). 이건
  사이트 실패가 아니라 **보류**이고, 요청하지 않았으므로 `timeoutStreak`도 올리지 않습니다.
  다음 전체 수집에서는 사이트별 백오프에 묶지 않고 다시 시험합니다. `only`로 한 선사만
  최신화할 때는 다른 선사까지 막을 수 없으므로 회로 차단을 걸지 않습니다.
- **한 계열이 통째로 죽는 건 그 계열 공통 원인입니다.** 더피싱은 `gapKey`가 `thefishing.kr`
  하나로 뭉쳐 96곳이 한 줄로 서고, detail 방식이라 사이트당 3요청(`days 21` / `windowDays 7`)
  이라서 한 백엔드에 288요청을 14분 넘게 계속 보냅니다. 선상24는 154곳이 같은 그룹이어도
  사이트당 1요청(7.7분)이라 매번 성공합니다. 더피싱만 죽으면 이 차이부터 의심하세요.

- **안 쓰는 칸은 접어둘 수 있습니다 — 자동으로 숨기지는 않습니다.** 승선료는 한 쪽 828줄 중
  777줄(94%)이 빈 채로 열 하나를 차지합니다. 그렇다고 화면이 알아서 숨기면 **값이 있는
  선사만 보는 사람이 고르는 기준을 잃고**, 데이터가 바뀔 때마다 열이 나타났다 사라집니다.
  그래서 `칸 고르기` 메뉴로 사람이 고르고, 고른 것은 즐겨찾기·별점처럼 그 브라우저에만
  남깁니다(`localStorage`의 `fishing:columns`). 메뉴에는 **그 칸이 지금 쪽에서 얼마나
  비었는지**를 같이 적습니다(`emptyShare`) — 접을지 말지 판단할 값입니다.
  숨기기는 `body[data-hide~="price"] .cell-price` 한 줄로 하므로 **머리글에도 줄과 같은 칸
  이름이 달려 있어야 합니다**(`<th class="cell-price num">`). 하나만 숨으면 표가 통째로
  어긋납니다. **그 규칙은 모바일 블록 밖에 둡니다** — 안에 넣으면 넓은 화면에서 안 먹습니다
  (한 번 그렇게 넣었다가 상태만 저장되고 칸은 그대로였습니다). `test/page.test.js`가 셋을
  다 지킵니다.
- **칸 순서가 곧 중요도입니다. 그리고 칸은 번호가 아니라 이름으로 다룹니다.**
  현황판 표는 왼쪽부터 `날짜 · 선사 · 배 · 상태 · 잔여 · 항구 · 어종 · 승선료 · 운항 · 구분`입니다
  (`docs/index.html`의 `cells`). 운항은 36.5%, 승선료는 95%가 빈 칸이라(`node quality.js`) 그것들이
  앞에 있으면 **첫 화면이 빈 칸으로 덮이고** 정작 고를 때 보는 값이 오른쪽으로 밀려 가로 스크롤
  뒤에 숨습니다. 그래서 잘 차는 값부터 왼쪽입니다. 순서를 바꾸면 **`<thead>`도 같이** 고치세요 —
  둘이 짝이고 한쪽만 고치면 값이 다른 머리글 밑으로 들어갑니다(화면은 멀쩡해 보이고 숫자만
  엉뚱한 칸에 찍힙니다). `test/page.test.js`가 두 목록을 맞춰 봅니다.
  칸마다 다른 처리(상태 배지·선사 링크·별점·nowrap·숫자 정렬)는 **이름으로 가릅니다**
  (`NOWRAP_CELLS`·`NUM_CELLS`). 예전처럼 `i === 7`로 두면 순서를 바꿀 때마다 그 분기가 조용히
  다른 칸에 걸립니다. 모바일 카드는 CSS grid 영역이라 순서를 바꿔도 그대로입니다.
- **표의 모든 칸에 `white-space: nowrap`을 걸지 마세요.** 자연 폭이 1487px까지 늘어서
  1440px 화면에서도 오른쪽 칸(관심 출조)이 잘렸습니다. 지금은 선사·배·항구·어종만 줄이
  갈리고(폭을 여기서 법니다), 날짜·운항·구분·상태·숫자 칸만 nowrap입니다.
- **제일 자주 하는 일은 줄을 따로 내줍니다 — 검색칸.** 배를 이름으로 찾는 것이 이 화면에서
  제일 자주 하는 일인데, 필터 여덟 개 뒤에 같은 모양으로 끼워 두니 **있는 줄도 몰랐습니다.**
  지금은 필터 줄 위에 `searchbar`로 따로 있고, 크기·돋보기 아이콘으로 구분하며, 검색어가
  들어 있으면 테두리·바탕이 강조색으로 바뀝니다(표가 걸러진 상태라는 표시입니다 — 필터가
  켜졌을 때와 같은 신호).
  **작은 화면에서는 검색칸과 필터를 한 덩어리(`.controls`)로 붙여 sticky로 둡니다.** 따로 두면
  스크롤할 때 검색칸만 밀려 사라집니다. 그 덩어리 높이에 아래 것들의 `top`이 얹혀 있으니
  (`.multi-menu`·`tr.dayhead`의 `161px` = 제목 55px + 덩어리 106px) **여기를 고치면 그 값도
  같이 재서 고치세요.** 브라우저에서 `.controls`의 `bottom`과 `tr.dayhead`의 `top`이 같은지
  보면 됩니다 — 어긋나면 날짜 머리글이 필터 밑으로 파고듭니다.
- **모바일은 같은 행을 카드로 재배치합니다.** 720px 아래에서 `triprow`의 칸을 CSS grid로
  놓아 배·선사·어종·운항과 상태·잔여석을 한 카드에 보여줍니다. 모바일용 목록을 따로 만들면
  필터 결과를 두 번 그리게 되고 별·감시 버튼 상태도 갈리므로 DOM은 하나만 둡니다. 배 사진은
  수집 데이터에 없고 원본 사이트 이미지를 복제하지도 않으므로 `boatmark`에 배 이름 첫 글자를
  씁니다. 선사 링크·최신 확인·감시 버튼은 작은 화면에서도 숨기지 마세요.
  **카드에서는 긴 글자 하나가 옆 칸을 짓누릅니다.** 잔여 칸은 숫자 칸이라 `nowrap`인데,
  거기 붙는 확인 문구가 길면("수집 보류 · 직전 4시간 46분 전 확인") 그 칸이 통째로 그만큼
  늘어나 선사 칸이 71px로 눌리고 전화번호가 그 위로 삐져나와 **겹쳐 찍혔습니다**(390px에서
  172장이 그랬습니다). 숫자는 한 줄로 두고 **확인 문구만** 접되 `max-width`로 폭을 막습니다 —
  접기만 해서는 칸이 여전히 제일 긴 줄만큼 넓어집니다. 한글은 기본값이 글자 단위로 갈려서
  `word-break: keep-all`을 같이 겁니다(안 그러면 "4시간"이 "4시 / 간"으로 끊깁니다).
- **화면 이동은 왼쪽 탭바 하나로 합니다.** 현황판과 시스템 관리는 파일이 둘이라(Pages에는
  `docs/`만 올라갑니다) 탭은 실제로는 링크이고, 지금 보는 쪽에 `aria-current="page"`를 답니다
  (`docs/index.html`·`docs/admin.html`의 `.sidenav`). 제목 아래에 있던 서로 가는 링크는
  없앴습니다 — 길이 둘이면 한쪽만 고치게 됩니다. **같은 조각이 두 파일에 들어가므로**
  한쪽만 고치면 화면마다 탭이 다른 자리에 뜹니다(즐겨찾기·별점이 그렇듯이).
  `test/page.test.js`가 두 탭바를 글자까지 맞춰 봅니다. 색은 각 화면의 `--accent`를 그대로 써서
  현황판에서는 파랗고 관리 화면에서는 주황입니다 — 어디에 있는지가 한 번 더 보입니다.
  **작은 화면에서는 아래로 내립니다** — 위쪽은 제목(`top: 0`)·필터(`55px`)·날짜 머리글(`108px`)이
  sticky로 자리를 잡고 있어서 탭바를 거기 얹으면 그 값이 전부 어긋납니다.
  **접으면 172px을 표에 돌려줍니다**(`navfold`). 관리 화면 표는 자연 폭이 1430px이고 현황판도
  칸이 열둘이라, 탭바가 늘 자리를 차지하면 오른쪽 칸이 잘립니다. 접은 상태는 즐겨찾기·별점과
  같은 급의 개인 기록이라 브라우저에만 두고(`localStorage`의 `fishing:nav-fold`), 열쇠가 하나라
  한쪽에서 접으면 다른 화면도 접혀 있습니다 — 화면마다 폭이 달라지면 오갈 때마다 놀랍니다.
  **접기 규칙은 `@media (min-width: 721px)`로 묶습니다.** 저장한 값은 화면 크기를 모르므로
  안 묶으면 접어둔 채로 폰에서 열었을 때 아래 탭바의 글자만 사라집니다.
- **날짜는 전부 한국시간입니다**(`core/when.js`). Actions 러너가 UTC라 그냥 두면
  06:10 수집 때 "오늘"이 어제가 됩니다. 새 코드에서 `new Date()`로 날짜를 만들지 마세요.
- **조사는 근거가 아니라 "붙일 수 있느냐"까지 말해야 합니다.** 근거만 쌓아두면 읽는 사람이
  매번 같은 판단을 다시 합니다. `core/research.js`의 `proposeGuides`가 붙일 `timeGuide` 초안과
  **막는 이유**를 같이 냅니다(`값이 여러 개` / `적용 기간(근거는 2025년 공지)` / `배 확인(2척)`).
  **값을 정해주지는 않습니다** — 막는 것이 없는 줄만 그대로 쓰고 나머지는 사람이 해결합니다.
  이유를 안 적으면 작년 공지의 시각이 올해 값으로 조용히 남습니다(홍원 2024·2025년도 공지가
  실제로 그럴 뻔했습니다). **적용 기간은 원문이 적어둔 것만 인정하고**, 연도만 있는 공지는
  그 연도를 알려주기만 합니다. 승선료는 여전히 제안하지 않습니다 — 선비·예약금·장비 대여료가
  섞여 있어 숫자만 보고는 못 고릅니다.
  **공지의 배를 알아볼 때는 registry와 수집 결과의 이름을 같이 봅니다.** registry에 배를
  안 적어둔 선사가 25곳(46척)이라 registry만 보면 그 곳들은 "배가 하나뿐"인 것처럼 보여
  못 집었는데도 경고조차 안 붙었습니다. `researchTargets`가 그 선사의 수집된 배 이름을
  `boats`에 실어 보내고 `proposeGuides`가 둘을 합쳐 봅니다.
- **손으로 눌러야만 도는 기능은 아무도 안 누릅니다.** 공식 사이트 정보 조사는 버튼이 하나
  있을 뿐이어서 돌린 사람이 없었습니다(항구 채우기를 주 1회 스스로 돌게 한 것과 같은 사정).
  이제 로컬 서버가 **6시간마다 우선순위 상위 3곳**을 스스로 조사하고(`serve.js`의
  `autoResearch`), 관리 화면은 열자마자 마지막 보고서와 "언제 스스로 보는지"를 적습니다 —
  돌기만 하고 아무도 안 읽으면 뜻이 없습니다. `RESEARCH_AUTO_HOURS=0`이면 끕니다
  (`RESEARCH_AUTO_LIMIT`·`RESEARCH_AUTO_PAGES`로 양도 조절합니다).
  **수집이 도는 중이면 그 차례를 건너뜁니다** — 기다렸다 끼어들면 3분 감시가 밀립니다.
  반대로 조사가 도는 동안에는 수집이 멈추므로(같은 예약 플랫폼을 두 프로세스가 동시에
  두드리지 않으려는 것) 간격을 넉넉히 둡니다. 손으로 누른 조사와 스스로 도는 조사는
  `beginResearch` 한 곳을 같이 씁니다 — 갈라 두면 한쪽만 고치게 됩니다.
  **스스로 돌 때는 선사를 고르지 않습니다**(`--limit`). `research.js`가 선사마다 7일(실패는
  6시간) 대기를 걸어두므로 자주 깨워도 같은 곳을 다시 두드리지 않습니다.
- **밖에 여는 길은 감시 목록 하나뿐입니다.** 상시 감시 서버는 밖에 둡니다(`DEPLOY.md`).
  그때도 열리는 것은 `/api/monitor`(자기 감시 읽기·쓰기)뿐이고, 그것도 `WATCH_PUBLIC=1`을
  **따로 켜야** 열립니다 — 주소를 0.0.0.0으로 바꿨다고 쓰기 길이 같이 열리면 안 됩니다.
  밖에서 온 요청은 `X-Watcher` 토큰이 있어야 받습니다(커스텀 헤더라 다른 사이트 폼이
  못 쏩니다). 판단은 `serve.js`의 `apiAccess` 한 군데서만 하고 테스트가 지킵니다.
- **꺼져 있는 버튼은 왜 꺼졌는지 말해야 합니다.** 관리 화면의 [변경 저장]은 꺼져 있는 이유가
  둘입니다 — 고친 값이 없거나(`DIRTY`가 빔), 읽기 전용이라 아예 못 쓰거나(`LOCAL`이 false).
  버튼만 보면 그 둘이 고장 난 것과 구별이 안 돼서 "이 버튼은 왜 있나"가 됩니다(실제로 그
  질문을 받았습니다). `paintSave()` 한 곳에서 문구·켜짐·설명을 같이 정하고, 상태가 바뀌는
  네 자리(고친 곳·저장 뒤·읽기 전용 전환·로컬 확인)에서 모두 부릅니다. 읽기 전용 배너는
  스크롤하면 안 보입니다 — 버튼 자신이 말해야 합니다. **버튼을 없애지는 마세요**: 표에서
  고친 표기·항구·전화·켜짐을 registry에 쓰는 유일한 길입니다.
- **저장 전에 고친 값은 표를 다시 그려도 남아야 합니다.** 관리 화면의 표는 정렬·필터·쪽
  넘김마다 통째로 다시 그리는데, 칸을 `site`에서 그대로 읽으면 방금 친 글자가 옛 값으로
  돌아갑니다. 값 자체는 `DIRTY`에 남아 [변경 저장]으로 들어가지만, 화면만 보는 사람에게는
  지워진 것이라 다시 칩니다. 그래서 그리는 쪽은 `editedValue(site, key)`로 **고친 값을 먼저**
  읽습니다(표기·항구·전화·켜짐 모두). 항구 지역순 정렬이 이 자리를 훨씬 자주 지나갑니다.
- **관리 API는 로컬 밖으로 새면 안 됩니다.** `/api/*`는 registry를 고치고 프로세스를 띄웁니다.
  루프백 바인딩 + Host 검사 + `X-Admin` 헤더 세 겹으로 막아뒀고(`serve.js`의 `adminAllowed`),
  `test/admin.test.js`가 이걸 지킵니다. 편집 가능한 필드도 표기·켜짐만입니다(`EDITABLE`) —
  `url`이나 `adapter`가 화면에서 바뀌면 수집이 통째로 죽습니다.
- **재시작은 종료 코드 75로 합니다.** 서버가 자기 자신을 새로 spawn하면 `run.bat` 창이
  주인을 잃고 서버만 백그라운드에 남습니다. 대신 75로 끝내고 실행기(`run.bat`의
  `:serve_loop`)가 다시 띄웁니다. `npm run serve`에는 루프가 없어 자동 재시작이 안 되므로,
  `/api/sites`가 `restartable`을 알려주고 화면이 버튼 문구를 거기에 맞춥니다.
- **배치 파일에서 `if 조건 A & B`는 B가 조건과 무관하게 실행됩니다.** 재시작 루프를
  이렇게 썼다가 무한 루프를 만들 뻔했습니다. 조건부 여러 줄은 괄호 블록으로 쓰세요.
  `rem` 줄의 `>`도 리다이렉션으로 해석됩니다(주석 하나가 data.json을 날릴 뻔했습니다).
- **`docs/data.json`은 봇과 사람이 같이 씁니다.** Actions가 수집할 때마다 커밋하고,
  로컬에서 `collect.js`(관리 페이지의 [수집 다시 실행] 포함)를 돌려도 같은 파일이 바뀝니다.
  그래서 로컬에서 한 번이라도 수집하면 pull이 매번 이렇게 막힙니다:
  `Your local changes to the following files would be overwritten by merge: docs/data.json`.
  `run.bat`은 받기 전에 이 파일을 `git checkout HEAD --` 로 버립니다 — 다음 수집 때 다시
  만들어지는 결과물이라 잃을 게 없습니다(`KEEP_DATA=1`이면 안 버립니다).
  **로컬 수집 결과를 남기고 싶으면 커밋해서 푸시해야 합니다.** 안 그러면 어차피 화면에도 안 올라갑니다.
- **같은 날을 사이트마다 다르게 부릅니다(물때).** 이름으로 부르는 곳(`한객기`·`대객기`·`조금`·
  `무시`)과 숫자로 부르는 곳(`12물`~`15물`)이 섞여 있고, 지역에 따라 하루씩 밀리기도 합니다
  (같은 날 `3물`과 `4물`이 같이 나옵니다). 수집 결과로 확인한 대응은
  **12물=한객기 · 13물=대객기 · 14물=조금 · 15물=무시**입니다(2026-09-16 하루에 `12물` 235건,
  `한객기` 150건이 같이 붙었습니다). **표기를 하나로 고쳐 쓰지 않습니다** — 어느 쪽도 틀린 게
  아니고 사이트가 적어둔 것을 바꿔 보여줄 이유가 없습니다. 대신 **판단은 표기와 무관해야**
  합니다(`docs/index.html`의 `isGoodTide`) — 안 그러면 같은 날인데 선사에 따라 "좋은 물때"
  배지가 붙었다 안 붙었다 합니다. 하루 오프셋 때문에 경계일(예: 11물/12물이 같이 있는 날)에는
  여전히 갈리는데, 그건 지역차라 맞는 동작입니다.
- **한글에는 `\b` 단어경계가 없습니다.** 배 이름 정규식은 `(?![가-힣])`로 끊습니다.
  이거 때문에 한 번 틀렸으니 정규식 고칠 때 조심하세요. **같은 이유로 뽑은 값을 낱말
  단위로 비교해서 거르면 안 됩니다.** 본문에서 "○○항"을 뽑을 때 `사항`·`운항`을 목록에
  두고 **같은지**로 쟀더니, 앞 글자가 붙어 나온 "공지사항"·"안전운항"이 그대로 통과해
  항구 후보 절반이 헛것이 됐습니다. 지금은 **그 말로 끝나는지**로 봅니다
  (`core/ports.js`의 `looksLikePort` — 뽑는 쪽 `discover.js`와 읽는 쪽 `core/quality.js`가
  같이 씁니다. 이미 registry에 적힌 옛 note는 그대로라 읽는 쪽에서도 걸러야 합니다).

## 작업 방식

**변경 하나 = 커밋 하나 = PR 하나.** 여러 변경을 한 브랜치에 쌓아두지 마세요.
사이트 하나 추가, 버그 하나 수정, 기능 하나 — 각각 브랜치를 따고 푸시한 다음
**곧바로 PR을 엽니다.** 물어보고 열지 말고 그냥 여세요.

**PR을 열었으면 CI를 기다렸다가, 통과했으면 머지하고 `main`을 pull까지 합니다.**
여기서도 물어보지 않습니다. 여는 것과 마찬가지로 그냥 합니다.

```bash
git checkout -b <작업이름>
# ... 변경 ...
npm test
git commit -m "..."
git push -u origin <작업이름>
# 푸시했으면 바로 PR 생성. 리뷰는 PR에서 합니다

# CI(ci 워크플로)가 초록이면
#   squash 머지 — 제목은 "<제목> (#N)"
git checkout main && git pull origin main
```

**이 흐름은 둘이 지킵니다 — 마무리 담당과 Stop 훅.**
`.claude/agents/finish.md`가 서브에이전트(`finish`)로, 코드 작업을 마친 뒤 부르면 테스트부터
머지·`main` 최신화·재수행까지 한 덩어리로 돕니다. 이름이 `ship`이 아닌 것은 **이 레포에서
`ship`은 배로 읽히기 때문**입니다. 위 단계를 사람이 기억해서 부르는
것으로는 안 굴러갑니다 — 실제로 커밋에서 멈춘 적이 있습니다. `.claude/hooks/ship-check.mjs`는
그물입니다: 턴 끝에서 `origin/main`과 비교해 안 들어간 커밋이 있으면 턴을 막고 남은 단계를
알려줍니다. **훅은 파일만 있다고 돌지 않습니다** — `.claude/settings.json`의 `hooks.Stop`에
적혀 있어야 실행됩니다. 실제로 그 등록이 빠져 있어서 훅이 한 번도 안 돌았고, 그 사이
PR을 안 여는 세션이 있었습니다. 한 번만 막으므로(`stop_hook_active`) 정말 멈춰야 할 때의
탈출구는 그대로입니다(`SKIP_SHIP_CHECK=1`도 있습니다).

**다른 프로젝트에서도 쓰려면 범용판을 복사합니다.** `.claude/portable-agents/finish.md`를
`~/.claude/agents/finish.md`로 복사하면 모든 프로젝트에서 `finish`가 뜹니다. 그 판은 이
레포를 모릅니다 — 테스트 명령(`package.json`·`Makefile`·`pyproject.toml`…)과 기본 브랜치,
그 프로젝트의 지침 파일을 **스스로 찾아 읽습니다.** 크게 다른 점 하나: **범용판은 기본적으로
머지하지 않고 멈춥니다.** "CI 초록이면 머지"는 이 레포가 정한 규칙이지 남의 저장소의 규칙이
아니라서, 그쪽에 그렇게 적혀 있거나 사람이 "머지까지"라고 했을 때만 머지합니다. 이름이 같아
이 레포에서는 언제나 `.claude/agents/` 쪽이 이깁니다(프로젝트가 사용자 설정보다 먼저).
**두 판은 목적이 달라 따로 삽니다** — 여기 것은 이 레포의 함정을 적어두는 곳이고, 그쪽은
아무것도 모르는 프로젝트에서 알아내는 순서를 적어두는 곳입니다. 이 레포의 규칙이 바뀌면
여기만 고치면 됩니다.

**받아오기가 실패하면 "얼마나 걸려서"를 먼저 보세요.** `describeError`가 `— N초 만에`를
같이 찍습니다. 30초를 기다린 것(상대가 붙여놓고 응답을 안 줌)과 2~5초 만에 끊긴 것
(연결·DNS·다른 데서 건 시계)은 손쓸 데가 완전히 다릅니다. 실제로 이걸로 잡았습니다 —
2026-09-16 `times`가 "30000ms 안에 응답이 없습니다"라고 하면서 **5.0초 만에** 죽었는데,
범인은 상대가 아니라 **Node의 기본 agent**였습니다(19부터 `keepAlive: true, timeout: 5000`).
그 5초가 소켓에 걸리면 우리가 건 30초는 무시되고, `req.setTimeout(30000, cb)`의 cb는
그대로 불려서 **틀린 숫자로 보고**합니다. 해외 러너에서 국내 호스트에 붙는 데는 5초가
모자랍니다. 그래서 `agent: false`로 기본 agent를 안 탑니다 — 같은 서버에 3초씩 쉬고
붙으므로(`pace`) 연결 재사용으로 얻을 것도 없습니다.

**`npm test`가 초록이어도 CLI는 따로 돌려보세요.** `discover.js`는 최상위에서 바로 명령을
돌립니다 — 파일 아래쪽에 `const`로 둔 헬퍼를 위쪽 명령이 먼저 쓰면 "Cannot access X before
initialization"으로 **즉시** 죽습니다(함수 선언은 통째로 끌어올려지니 안전합니다). 조각
테스트는 모듈을 import한 뒤에 부르니 이걸 못 잡습니다. 2026-09-16 `times` 워크플로가
이렇게 0초 만에 실패했습니다. 지금은 `test/smoke.test.js`가 `ports`·`times`·`prices`를
`--limit 0`으로 진짜 실행해 봅니다(네트워크는 안 탑니다).

**머지는 squash로 합니다.** 브랜치에 커밋이 몇 개든 `main`에는 한 줄로 남습니다.
어차피 변경 하나에 PR 하나라, `main` 이력이 곧 변경 목록이 됩니다.

**CI가 빨간 PR은 머지하지 않습니다.** 실패를 고쳐서 다시 푸시하고, 초록이 된 다음에
머지합니다. CI와 무관하게 판단이 필요한 변경(설계가 갈리는 것, 되돌리기 어려운 것)은
머지 전에 물어보세요 — 자동 머지는 **CI로 확인이 끝나는 변경**에 대한 것입니다.

머지하고 pull까지 해야 다음 작업이 최신 `main` 위에서 시작합니다. 이걸 빼먹으면
브랜치가 옛날 `main`에서 갈라져 나와 쓸데없는 충돌이 납니다.

**커밋에서 멈추지 마세요.** 커밋했으면 푸시 → PR → CI 확인 → 머지 → `main` 최신화까지가
한 덩어리입니다. "커밋해뒀습니다"로 끝내면 `main`에는 아무것도 안 들어가고, GitHub Pages는
`main`의 `/docs`를 보므로 화면도 그대로입니다. 중간에서 손을 떼는 건 CI가 빨갛거나
아래 예외에 해당할 때뿐이고, 그때는 무엇 때문에 멈췄는지 말해야 합니다.

자동 발견(`discover`)이 낸 registry PR도 같습니다 — CI가 초록이면 머지합니다. 잘못 붙은
줄은 나중에 registry에서 지우면 되고, 그게 안 올라가 있으면 확인할 수조차 없습니다.

**`core/fetcher.js`를 건드렸으면 머지 전에 `peek`을 한 번 돌리세요.** 테스트는
네트워크를 타지 않기 때문에, 실제 요청이 깨지는 변경은 수집에서만 드러납니다.
(로컬 서버로 도는 `test/fetcher.test.js`가 상당 부분 잡아주지만, 실제 사이트까지는 아닙니다.)

이미 열려 있는 PR에 후속 커밋을 얹는 건 괜찮습니다 — 같은 변경을 마무리하는
경우에 한해서입니다. 성격이 다른 변경이면 브랜치를 새로 땁니다.

푸시만 하고 PR을 안 열면 `main`에 아무것도 안 들어갑니다. GitHub Pages는
`main`의 `/docs`를 보므로, PR이 머지되기 전까지 화면은 그대로입니다.

## 선사를 늘릴 때

전국 선사는 수백 곳입니다. 하나씩 찾아 registry에 적는 건 오래 못 갑니다. `discover.js`가
후보를 모으는 일과 그 후보가 우리 어댑터로 읽히는지 확인하는 일을 대신합니다.

```bash
node discover.js wayback sunsang24.com  # 웹 아카이브에 남은 그 플랫폼 서브도메인 전부
node discover.js ct sunsang24.com     # 인증서 로그(crt.sh) — 와일드카드 인증서를 쓰는 플랫폼에는 안 통합니다
node discover.js links <목록 페이지>    # 그 페이지에 걸린 선사 홈페이지 도메인
node discover.js probe --from tmp/candidates.json        # 어댑터로 실제 돌려보기
node discover.js probe --from tmp/candidates.json --add  # 읽히는 곳만 registry에 붙이기
```

모으는 부분(`ct`/`links`)과 확인하는 부분(`probe`)이 나뉘어 있습니다. 후보를 어디서
얻느냐는 소스마다 다르고 자주 바뀌지만, "이 주소가 읽히느냐"는 어디서 왔든 같습니다.
새 소스가 생기면 앞부분만 붙이면 됩니다.

**개발 환경에서는 crt.sh도 국내 도메인도 막혀 있습니다.** Actions 탭 → discover →
Run workflow로 돌리세요. `open_pr`을 켜면 읽히는 후보를 registry에 붙인 PR까지 엽니다.

**자동으로 채운 값을 그대로 믿지 마세요.** 특히 `port`·`phone`은 다른 사이트에 올라온
같은 배를 알아보는 신원입니다(`core/merge.js`). 그래서 `discover.js`는 "출항지 : ○○항"처럼
라벨이 붙어 답이 하나일 때만 값으로 채우고, 애매하면 비운 채 후보만 note에 적습니다.
PR을 머지하기 전에 이걸 확인하는 게 사람이 할 일입니다 — 합쳐서 틀리는 것보다 두 줄이 낫습니다.

## 항구를 채울 때

지금 남은 병목입니다. 배 473척 중 항구가 적힌 것이 65척뿐이라(`node quality.js`)
같은 배가 현황판에 두 줄로 뜹니다. 항구는 registry에 사람이 적는 값이라 저절로
채워지지 않습니다.

```bash
node quality.js                 # 어디부터 채워야 하나 — 지금 두 줄로 뜨는 배와 그 선사
node discover.js ports          # 항구 빈 선사의 페이지를 다시 읽어 후보 보기
node discover.js ports --add    # 라벨로 찾아 답이 하나인 곳만 registry에 채우기
```

**순서가 곧 이 기능입니다.** 항구 빈 곳을 id 순으로 훑으면 지금 당장 손해를 보는 곳이
뒤에 묻힙니다. `portTargets`는 `quality.js`가 세어둔 "합치기가 막힌 곳"을 앞에 두고,
그다음은 출조가 많은 곳입니다 — 한 곳을 채워 여러 줄이 고쳐지는 순서입니다.

**관리 화면에서도 같은 순서로 볼 수 있습니다** — 사이트 표의 `정렬 · 항구 지역순`
(`docs/admin.html`의 `byPort`). 채우는 일은 지역 단위로 몰아서 하는데, 표가 id 순이면
같은 지역 선사가 300곳에 흩어져 페이지를 몇 번씩 다시 찾습니다. 지역을 읽는 규칙
(`regionOf`)은 **현황판과 글자까지 같아야** 합니다 — 갈리면 관리 화면에서 "충남 보령"으로
묶어 채운 것이 현황판 지역 필터에서는 다른 묶음이 됩니다(`test/admin.test.js`가 두 함수를
맞춰 봅니다). **항구를 모르는 곳은 방향과 상관없이 뒤로** 갑니다(현황판의 `byValue`와 같은
규칙입니다) — 빈 줄이 첫 쪽을 덮으면 지역별로 보려던 것을 정작 못 봅니다. 줄 세우는 값은
`hasPort`와 같은 기준이라 **배별로만 적어둔 항구도 셉니다**(`gull`·`mansour`·`urbis` 셋).
그 줄은 사이트 항구 칸이 비어 있어 남의 지역에 끼어 보이므로, 칸 밑에 `배별 ○○항`으로
줄 세운 근거를 적어둡니다. 고치는 칸은 그대로 사이트 항구입니다 — 배별 값은 registry를
손으로 고칩니다.

**정렬을 여는 문은 둘입니다 — 정렬 칸과 `항구` 머리글.** 칸을 표 위에 뒀더니 못 찾고
"정렬이 없다"는 말이 나왔습니다. 사람은 **칸 머리글을 눌러** 정렬하려 듭니다. 그래서
머리글도 같은 정렬을 걸고(한 번 더 누르면 등록순으로 돌아옵니다), 지금 어느 정렬인지는
`aria-sort`와 화살표로 보입니다. **값을 정하는 곳은 `setSort` 하나뿐입니다** — 갈리면
머리글로 정렬해 놓고 정렬 칸에는 "등록순"이라고 적혀 있게 됩니다.

**현황판에도 `항구순`이 있습니다**(`docs/index.html`의 `byText`). 다만 **하루 안에서만**
겁니다 — 표가 날짜로 끊겨 있고 쪽도 날짜로 나누므로 통째로 섞으면 날짜 머리글이 줄마다
나옵니다(위의 "정렬은 하루 안에서만" 항목). 항구 이름에 지역이 앞에 붙어 있어 가나다순이
곧 지역별 묶음이고, 항구를 모르는 줄은 `byValue`와 같은 규칙으로 뒤로 갑니다. 지역을
통째로 보려면 정렬이 아니라 **지역·항구 필터**입니다.

**못 받은 것과 안 적혀 있는 것은 다르게 셉니다.** 앞은 다시 돌리면 되고 뒤는 사람이
페이지를 봐야 합니다. 값이 없다는 것만 알려주면 상대가 막고 있는 판에 "전부 페이지에
안 적혀 있음"으로 읽습니다 — 여기서 한 번 그렇게 보고 헷갈렸습니다.

**전화번호도 같은 도구가 같이 채웁니다.** 페이지를 한 번 받아 항구와 전화를 같이 읽는데
(`identity`) 항구만 쓰고 전화를 버리고 있었습니다 — 그래서 **전화만 빈 49곳은 아무도 안
봤습니다.** 지금은 둘 중 하나라도 비면 대상이고(`portTargets`의 `needs`), 빈 칸만 채웁니다.
전화는 **페이지에 번호가 하나뿐일 때만** 값이 됩니다(`pickPhone`) — 여럿이면 어느 것이 예약
번호인지 모르고, 틀린 번호로 합치면 다른 배가 한 줄이 됩니다. 다른 사이트에 같은 이름의 배가
있다고 그쪽 번호를 베껴 오지는 않습니다. 그러면 이름만으로 합치는 셈이고, 그게 `core/merge.js`가
막으려는 일입니다.

여기서도 국내 도메인이 막혀 있으므로 **Actions 탭 → ports → Run workflow**로 돌립니다.
`open_pr`을 켜면 채운 값으로 PR까지 엽니다. 값으로 못 쓴 후보는 아티팩트(`tmp/ports.json`)에
전부 들어 있으니, 사람이 고를 때 거기부터 보면 됩니다.

**이제 주 1회 스스로 돕니다**(화요일 새벽, `ports.yml`의 `schedule`). 사람이 기억해서
돌리는 것으로는 안 굴러가서입니다 — 항구가 빈 배가 100척인데 아무도 안 채우면 영영 빕니다.
스케줄 실행은 `--add`로 돌아 PR까지 열지만 **머지는 사람이 합니다.** `times`·`prices`도
같이 돌되 그 둘은 `--add`가 없어 아티팩트만 쌓입니다(승선료는 자동 입력을 안 하기로 한
값입니다). 스케줄 실행에는 `workflow_dispatch`의 `default`가 안 실려서 `limit`은
워크플로 안에서 기본값을 답니다 — 비워 두면 `--limit ""`가 되고 `Number('')`는 0이라
한 곳도 안 보고 조용히 끝납니다.

**`port`는 `sites/ports.json`의 열쇠까지 적어야 합니다** — "남당항"이 아니라
"충남 홍성 남당항". 신원이면서(`core/merge.js`) 지도 핀의 열쇠라(`core/ports.js`),
맨 이름으로 적으면 좌표를 못 찾아 그 배들이 지도에서만 조용히 사라집니다. 그런데 선사
페이지는 "남당항"이라고만 적고 시·도·시·군은 옆의 주소에 있습니다. 그래서 `pickPort`는
**라벨 붙은 주소에서 시·군을 읽어**(`오시는길 : 충남 홍성군 서부면 …`) 아는 항구 목록과
맞춰보고, 한 곳으로 모일 때만 값으로 씁니다. 라벨 없이 본문에서 주운 이름은 주소와
맞아야만 값이 됩니다 — 안 그러면 조황글의 "오천항 앞바다까지 갑니다"가 출항지가 됩니다.

**그 "한 곳으로 모일 때"가 조용히 틀리는 경우가 있습니다.** 주소의 시·군에 등록된 항구가
`sites/ports.json`에 하나뿐이면, 실제 출항지가 그 목록에 **없는** 항이어도 등록된 쪽이
혼자 남아 이깁니다. 2026-09-16 ports 실행(run 35079170026)에서 세 곳이 그랬습니다 —
`nara`·`newhanlyo`는 주소가 여수 신월동/신월1길인데 신월항이 목록에 없어 "전남 여수 국동항"이
됐고, `bikini`는 페이지에 대천항·회변항이 같이 나오는데 회변항이 목록에 없어 "충남 보령 대천항"이
됐습니다. 셋 다 되돌렸고 note에 이유를 적었습니다.

**전화번호는 계좌번호와 못 가립니다 — 사람이 봐야 합니다.** 선상24 템플릿은 CUSTOMER
CENTER에 전화를, BANK INFORMATION에 계좌를 나란히 놓는데 둘이 닮았습니다. 나라2호는
전화 `010-4658-0007` / 수협 계좌 `0010-8601-1474`이고, 가가호는 **전화와 계좌 숫자가
아예 같습니다**(`010-2036-2003` / `0010-2036-2003`). 그래서 "계좌 옆 숫자는 버린다" 같은
규칙을 넣으면 가가호의 진짜 전화를 버립니다. 후보가 둘 이상이면 자동으로 안 채우는 지금
규칙이 맞고, 고를 때는 **"예약문의"라고 적힌 쪽**을 보세요.

**`bikini`가 걸린 갈래는 이제 코드가 막습니다**(`portNameKnown`). 모임 경로는 아는 이름만
세고 있던 것인지부터 봅니다 — 본문 후보 중에 `sites/ports.json`이 **모르는 이름**이 하나라도
있으면 값으로 안 씁니다. 목록에 없는 이름은 "다른 지역 것이라 빠진" 게 아니라 "우리가 모르는"
것이고, 같은 지역일 수도 있어서입니다. 지나가는 말로 적힌 다른 지역 항구는 안 걸립니다 —
그건 목록에 있어서 시·군이 다르다는 것까지 아는 이름이라, 근거가 아니라고 보여줄 수 있습니다.
라벨 경로는 이 관문을 안 지납니다(라벨이 그 자체로 근거이고, 라벨 안에 둘을 적어둔 경우는
이미 따로 막습니다). 항구의 갈래를 가리키는 말(국가어항·연안항·무역항)은 `NOT_PORT`에
넣었습니다 — 이름이 아니라 등급이라, 남겨두면 "모르는 항구"로 읽혀 공연히 막습니다.

**`nara`·`newhanlyo`가 걸린 갈래는 코드로 못 막습니다.** 그 두 페이지에 적힌 항구 이름은
국동항 **하나뿐**이고, 신월항은 아예 안 적혀 있습니다(주소가 "신월1길"일 뿐입니다). 알고리즘이
볼 수 있는 것만 보면 국동항이 맞고, 어긋난다고 판단한 것은 사람이 주소를 읽어서입니다.

그리고 **사람이 보니 알고리즘이 맞았습니다.** `newhanlyo`는 배너에 "여수 국동항 선상낚시
전문출항!", 배 사진 뱃지에 "국동항 한려피싱"이라고 박혀 있어 `전남 여수 국동항`으로 다시
채웠습니다 — 오시는길의 신월동 주소는 **출조점**이고 배는 국동항에서 뜹니다. 여수·통영처럼
출조점(낚시점·사무실)과 승선지가 떨어진 곳에서는 **주소가 승선지가 아닙니다.** 주소만 보고
어긋났다고 판단하지 말고, 배너·선박소개·조황글의 "○○항 출항"까지 보세요. 되돌릴 때도 값을
지우고 끝내지 말고 **되돌린 이유를 note에 적으세요** — 다음 실행이 같은 값을 다시 올립니다.

그 두 페이지는 여기서 열 수 없고(국내 도메인 차단) 사람의 PC에서도 선상24가 막히는 일이
있는데, **폰(통신사망)으로는 열립니다.** 차단은 사이트가 아니라 나가는 IP 쪽입니다.

그래서 **ports PR을 볼 때는 값이 라벨에서 나왔는지(`출항지 : ○○항`) 이 "모임" 경로에서
나왔는지부터 가르고, 모임 경로면 주소 줄과 항구가 같은 동네인지 눈으로 보세요.** 실행 로그의
`|` 줄은 값을 뽑은 줄이 아니라 근거 후보 4줄(`portEvidence`)이라, 거기에 항구 이름이 없다고
틀린 것도 아니고 있다고 맞는 것도 아닙니다. 애매하면 비우는 쪽이 낫습니다 — 잘못 합쳐진
한 줄보다 안 합쳐진 두 줄이 고치기 쉽습니다.

## 승선료를 채울 때

승선료는 어댑터가 아니라 registry의 `priceGuides`·`price`·`prices`에서만 옵니다. 홈페이지에는 선비와
예약금·입금액·장비 대여료가 같이 적혀 있어 숫자만 자동 입력하지 않습니다.

```bash
node quality.js                 # 승선료가 많이 빈 선사 순서 확인
node discover.js prices        # 승선료라고 명시된 근거 문장과 출처 모으기
```

개발 환경에서 페이지가 막히면 **Actions 탭 → prices → Run workflow**로 돌리고
`tmp/prices.json` 아티팩트를 봅니다. 도구는 예약금·입금·추가요금 문장을 제외하고 후보만
냅니다. 사람은 배·어종·시즌 차이를 확인한 뒤 registry에 적습니다. 자동 `--add`는 없습니다.

같은 어종도 오전/오후/종일·출항시각에 따라 금액이 다르면 배의 `priceGuides`에 적습니다.
규칙은 위에서부터 처음 맞는 것을 쓰므로 구체적인 규칙을 먼저 둡니다. `validFrom`·
`validThrough`·`source`·원문을 적은 `note`가 모두 있어야 적용됩니다. `species`는 문자열이나
대안 배열, `sessions`는 `오전`·`오후`·`종일`·`야간`, `departAt`은 `HH:MM` 문자열이나 배열입니다.
출조에 개별 시각이 없어서 조건을 확인할 수 없으면 그 규칙은 적용하지 않습니다.

**중복 감사는 수집 워크플로가 매번 돌립니다**(collect.yml의 `중복 감사` 단계). 사람이
기억해서 돌리는 것으로는 안 굴러갑니다. 로그에 "자동으로 정리할 중복"이 뜨면 `--disable`로
끄고, "사람이 봐야 하는 것"은 `--why`로 갈라 보세요.

**늘리고 나면 중복을 한 번 보세요.** 선상24는 배마다 서브도메인을 주는데 어느 주소로
들어가든 `/ship/schedule_fleet`이 함대 전체를 돌려줍니다. 그래서 한 선사의 주소가 둘 등록되면
같은 출조가 현황판에 두 줄로 뜹니다. `core/merge.js`는 못 막습니다 — 합치려면 전화번호가
있어야 하는데 대부분 없습니다. 없는 값을 지어내느니 같은 일정표를 두 번 긁지 않는 게 맞습니다.

```bash
node dupes.js            # 무엇을 끌 수 있는지 (수집 결과를 봅니다 — 먼저 collect가 돌아 있어야 합니다)
node dupes.js --disable  # 판정이 끝난 곳을 registry에서 끄기
node dupes.js --all      # 사람이 봐야 하는 쌍까지 전부
node dupes.js --why a b  # 그 두 곳이 어디가 어떻게 다른지 — 값이 갈린 쌍을 가릴 때
```

판정은 이름이 아니라 값으로 합니다. 겹치는 배들의 출조가 날짜·출항시각·잔여석·정원·어종까지
전부 같아야 같은 일정표로 보고, 하나라도 다르면 동명이배일 수 있으니 끄지 않고 목록에만 올립니다.
남기는 쪽은 배를 가장 많이 주는 곳입니다 — 끄는 쪽 배가 남는 쪽에 다 있을 때만 끕니다.

**배 수·출조 수까지 같으면 도구는 id 순으로 답합니다.** 늘 같은 답이 나오게 하려고 그런 것이고,
어느 쪽 데이터가 더 나은지는 모릅니다. 그때는 **신원이 더 확실한 쪽을 남기세요** — 전화번호가
있는 쪽, 배별 항구가 확인돼 있는 쪽입니다. `mansour`/`urbis`가 그랬습니다(7척·104건이
전부 같고 한쪽에만 있는 출조가 0건). 도구는 id 순으로 `mansour`를 남기라고 했지만, 남긴
것은 `urbis`입니다 — 예약 전화가 있고 어비스호(신시도항)·바다하리호(비응항)가 배별로
확인돼 있었습니다. 고른 이유는 끈 쪽 note에 적습니다.

**함대 일정표 사이트에 사이트 공통 `port`를 두면 다른 항 배까지 그 항으로 찍힙니다.**
`pickPort`는 `boats[배].port`가 없으면 `site.port`로 떨어집니다(`core/schema.js`). 배 하나짜리
사이트나 한 항구 함대에서는 그게 맞지만, `mansour`는 `boats`에 만수르호만 적어둔 채 사이트
공통 `비응항`을 두고 있어서 함대로 들어온 7척 전부가 비응항이 됐습니다 — **신시도항인
어비스호까지**. 함대가 여러 항에 걸친 곳은 사이트 공통값을 두지 말고 **배별로** 적으세요.
끄는 사이트도 마찬가지입니다: 다시 켜면 되살아나므로 끌 때 그 값을 배별로 내려 적습니다.

값이 갈린 쌍은 `--why`로 어느 필드가 몇 건이나 어긋나는지 봅니다. 여기서 판정이 갈립니다 —
**잔여석·상태가 같고 seatsTotal·물때만 다르면** 한쪽 파싱이 덜 된 같은 배이고(끄기 전에 파서를
고치는 게 먼저입니다), **잔여석이 죽 다르면** 다른 지역의 동명이배이거나 두 사이트를 받은 시각
사이에 예약이 들어온 것입니다.

## 어댑터 고칠 때

```bash
node debug.js              # 등록된 id 목록
node debug.js akbari       # 한 곳만
node debug.js akbari --dump  # 실패 시 원본 HTML을 tmp/ 에 저장
npm test                   # 가상 HTML로 파서 회귀 확인
```

`debug.js`가 "잔여석이 전부 비었다", "날짜가 하루뿐이다" 같은 흔한 증상을 짚어줍니다.

개발 환경에서 국내 도메인이 막혀 HTML을 못 볼 때는 **Actions 탭 → peek → Run workflow**에
사이트 id를 넣으면 됩니다. 러너가 받아온 페이지 모양(표기·태그 수·본문 앞부분)이 로그에 찍히고,
원본 HTML은 아티팩트로 올라옵니다.

`--dump`와 `--peek`은 `site.url`이 아니라 **어댑터가 실제로 긁는 주소**를 봅니다
(각 어댑터의 `targets()`). sunsang24는 메인이 아니라 `/ship/schedule_fleet`에 일정이 있습니다.

## 아직 확인 못 한 것

`sites/registry.json`의 실제 사이트들은 **국내 IP에서 한 번도 검증되지 않았습니다.**
개발이 이뤄진 환경에서 해당 도메인이 전부 차단(HTTP 403)돼서 실제 HTML을 못 봤습니다.
`test/fixtures.js`는 실제 마크업이 아니라 본문 표기만 흉내 낸 것이라, 테스트가 통과해도
진짜 사이트에서 되는지는 별개입니다. **각 사이트를 처음 켤 때 `node debug.js <id>`로
한 번씩 대조하세요.** 특히:

- sunsang24 목록형이 한 페이지에 며칠치를 보여주는지. 주소는 `/ship/schedule_fleet` 하나만
  부릅니다 — 여러 선사 사이트에서 확인한 형태입니다. 뒤에 숫자가 붙은 경우(`/1359`, `/0`)도
  있는데 연월이 아니라 배 번호로 보입니다. 다음 달까지 주소로 넘길 방법이 있으면
  registry에 `monthPath`를 적으면 그때부터 달을 넘겨가며 받습니다
- ~~더피싱 index 방식에서 메인 요약표를 실제로 찾는지~~ — 확인했습니다. **못 찾습니다.**
  메인에 "선박예약현황" 글자는 있지만 표를 JS로 그려서 정적 요청에는 안 들어옵니다
  (`node debug.js linkus --peek`가 예약현황이 든 `<table>`을 하나도 못 찾습니다).
  그래서 더피싱 사이트는 전부 `"source": "detail"`로 적어둡니다 — 안 적으면 매 수집마다
  헛된 요청이 사이트당 한 번씩 더 갑니다(115곳이면 5분이 넘습니다). discover가 새로
  붙이는 더피싱 항목에는 자동으로 들어갑니다.
- detail 방식은 `seatsTotal`이 정확해야 잔여석이 맞습니다

## 알아둘 것

- GitHub Actions 러너는 해외 IP입니다. 국내 사이트가 차단하면 그 사이트만 국내 서버로 옮기세요.
- 요청 간격은 호스트당 3초(`core/fetcher.js`의 `MIN_GAP_MS`). 사이트가 민감하면 늘리세요.
- **연결 제한시간은 30초**(`CONNECT_TIMEOUT_MS`). Node 기본값 10초로는 해외 러너에서 국내
  호스트에 붙다가 `UND_ERR_CONNECT_TIMEOUT`이 무더기로 납니다. 실제로 한 번 겪었습니다.
- 워크플로는 브라우저가 필요할 때만 playwright를 받습니다(`core/platform.js`의 `needsBrowser`).
  `"auto"`도 본문이 비면 브라우저로 넘어가므로 같이 셉니다 — `"js"`가 없다고 안전한 게 아닙니다.
- 취소석은 몇 분 단위 경쟁입니다. 알림을 진지하게 쓸 거면 상시 서버에서 5분 주기로 도는 게 맞습니다.
- 로컬 서버는 `core/monitor.js`로 전체 60분 / 관심 출조 3분 목표로 수집합니다.
  감시 대상은 현황판에서 배·날짜·운항별로 고르고 `tmp/monitor.json`에 저장합니다.
  로컬 수집 원문도 여기에 저장하며 `/data.json`과 관리 API는 이 결과를 제공합니다.
  현황판의 **선사 최신화**는 `/api/collect/:siteId`로 그 선사만 다음 수집 대상으로 만듭니다.
  합쳐진 출조는 출처를 먼저 고릅니다. 이 길은 전체 수집과 같은 로컬 관리 권한으로만 열고,
  공개 감시 API에는 포함하지 않습니다.
  관리 화면의 수동 수집도 같은 스케줄러를 거칩니다. 별도 프로세스를 띄워 간격 제한을 우회하지 마세요.
  GitHub Actions는 매시 10분에 전체 수집하며 기존처럼 `docs/data.json`을 커밋합니다.
