# 처음 온 사람에게 시켜볼 것 (실사용 검증)

기능이 있는 것과 사람이 쓸 수 있는 것은 다릅니다. 이 파일은 **옆에서 설명하지 않고**
지켜보기 위한 과제 목록입니다. 말을 보태고 싶어지는 순간이 곧 화면이 부족한 지점입니다.

## 어떻게 하나

- 과제 문장을 **그대로 읽어주고** 화면을 넘깁니다. 어디를 눌러야 하는지는 말하지 않습니다.
- 다 하거나, 30초 이상 막히거나, "이건 어떻게 해요?"라고 물으면 그 과제는 끝입니다.
- 기록할 것: **끝냈나 / 몇 초 걸렸나 / 어디서 멈췄나 / 무엇을 물었나.**
- 감시·알림 과제는 서버가 떠 있어야 합니다(`DEPLOY.md`). GitHub Pages만 열면 읽기 전용이라
  감시 버튼이 접힙니다 — 그 상태로 시키면 과제가 아니라 함정입니다.

## 과제 1 — 날짜·지역·어종으로 찾기

> "이번 주말에 갈 만한 주꾸미 배를 찾아서, 자리가 몇 개 남았는지 말해주세요."

- 관찰: 필터를 쓰나, 아니면 표를 위에서부터 훑나. 날짜를 어떻게 좁히나.
- **실패로 볼 것**: 지역 필터를 걸었더니 배가 대부분 사라져서 당황하는 것.
  지금 출조의 88%가 항구 값이 없어 `(항구 미상)`으로 들어갑니다(`node quality.js`).
  이건 사용자 잘못이 아니라 데이터 구멍입니다 — 그 반응이 나오면 그대로 적어두세요.
- 미리 알아둘 것: 어종은 주꾸미·갑오징어가 기본으로 켜져 있습니다. 다른 어종을 찾는
  과제로 바꾸면 **필터가 켜져 있다는 사실을 알아차리는지**를 같이 볼 수 있습니다.

## 과제 2 — 선상24 밖만 보기

> "선상24 말고 다른 데서 파는 배만 보고 싶어요. 그렇게 해보세요."

- 이 제품의 존재 이유가 이 과제입니다(`PRODUCT.md`). 못 하면 나머지가 다 무의미합니다.
- 관찰: 맨 앞 `플랫폼` 필터를 찾는 데 걸리는 시간. `더피싱`·`자체`를 **골라서 남기는**
  방식을 이해하는지(선상24를 빼는 게 아니라 나머지를 고르는 것입니다).
- **실패로 볼 것**: 선사 이름 옆 계열 배지를 보고도 필터를 못 찾는 것. 그러면 배지가
  "정보"로만 있고 "길"로는 안 보인다는 뜻입니다.

## 과제 3 — 마감된 출조 감시 걸기

> "이 배는 이미 만석인데, 자리가 나면 알고 싶어요. 그렇게 해두세요."

- 관찰: `3분 감시` 버튼을 찾는지. 누른 뒤 **무슨 일이 일어나는지 이해하는지**
  (버튼이 `감시 해제`로 바뀌는 것 말고 다른 신호가 없습니다).
- **실패로 볼 것**: 감시를 걸어놓고 "이제 어디서 확인해요?"라고 묻는 것.
  `[감시 중인 출조 관리]`와 `[내가 받은 알림]`이 접힌 채로 있어서 안 보일 수 있습니다.
- 같이 볼 것: 알림 채널(텔레그램·디스코드)이 설정돼 있지 않으면 화면에 그렇게 뜹니다.
  그 문구를 읽고 "그럼 알림이 안 온다는 거네"까지 이해하는지.

## 과제 4 — 알림에서 예약처까지 가기

> "(알림 한 건을 보여주고) 이 자리를 실제로 예약해보세요. 결제 직전까지만."

- 관찰: 알림의 링크를 누르는지. 열린 페이지에서 **그 날짜를 찾는 데 헤매는지**.
- 지금 링크는 두 종류입니다(`README.md`) — 그 날짜 예약 화면으로 바로 가는 것과,
  일정표로 가서 날짜를 직접 골라야 하는 것. 후자는 링크 옆에 `일정표`라고 적혀 있습니다.
  **그 표기를 읽고 대비하는지**가 이 과제의 핵심입니다.
- **실패로 볼 것**: 일정표에서 엉뚱한 날짜의 예약을 시작하는 것. 그러면 표기가 약한 것입니다.

## 무엇을 고칠지 정하는 법

- 세 명 중 두 명이 같은 곳에서 막히면 그건 사람 문제가 아니라 화면 문제입니다.
- 데이터 구멍(항구·승선료)과 화면 문제를 **섞지 마세요.** 항구가 없어서 못 찾은 것은
  registry를 채워야 할 일이고(`node quality.js`), 필터를 못 찾은 것은 화면을 고칠 일입니다.
- 관찰 결과는 `TODO.md`에 항목으로 올리되, **몇 명 중 몇 명이 막혔는지**를 같이 적으세요.
  숫자가 없으면 다음에 또 "이게 불편한 것 같다"로 돌아갑니다.

## 지금 알려진 함정 (관찰 전에 알고 있을 것)

| 함정 | 어디서 드러나나 | 근거 |
|---|---|---|
| 출조의 88%에 항구가 없음 | 과제 1의 지역·항구 필터 | `node quality.js` |
| 승선료가 99% 비어 있음 | 가격순 정렬이 기본순과 같아 보임 | `node quality.js` |
| 감시·알림은 서버가 있어야 함 | 과제 3·4 전체 | `DEPLOY.md` |
| 같은 배가 두 줄로 뜰 수 있음 | 과제 1에서 "이거 아까 봤는데?" | 신원 없는 배 35척, `node quality.js` |
| 물때 표기가 사이트마다 다름 | 날짜 줄에 `12물 / 한객기`가 같이 뜸 | `AGENTS.md` |
